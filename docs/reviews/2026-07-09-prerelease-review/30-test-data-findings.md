# Findings — 30-test-data (appended per batch; raw JSON in 30-test-data-raw/)

## Batch D1-licensing-philos-haxr

### unit: licensing-provenance (4 findings)

- **[HIGH/CONFIRMED]** CC-BY-NC-SA-4.0 (non-commercial) philos derivatives are committed to git and actively used by the test suite
  - `tests/fixtures/philos/labels/car_carrier_near_labels.csv:?` (licensing-blocker, confidence ?)
  - description: The R/V Philos dataset is recorded in data/README.md (line 18, 347, 380) as CC-BY-NC-SA-4.0 — NonCommercial + ShareAlike, exactly the license class the project deliberately rejected for MOANA and Global Fishing Watch. Despite this, a substantial body of philos-DERIVED artifacts is tracked in git and consumed by tracked tests: the three video-derived existence-label fixtures (tests/fixtures/philos/labels/{car_carrier_near,close_approach,sunset_cruise}_labels.csv, each header states 'Video-derived ... labels for the philos <clip>'), tests/fixtures/philos/charts/radar_structure_points.geojson (Boston radar-clutter points extracted from philos radar), charts/philos_cluster_classification.csv/.png and charts/philos_chart_coverage.png, plus ~10 docs/baselines/*philos*.csv metric dumps. tests/replay/test_philos_*_labels.cpp and PhilosLabelReplay.hpp validate the tracker against these NC-SA-derived labels. Adaptations of CC-BY-NC-SA material inherit NonCommercial + ShareAlike, so shipping them in a repo whose stated goal is commercial usability is a licensing conflict, and the ShareAlike term would virally attach to anything built from them. This also flatly contradicts tests/fixtures/philos/README.md's own invariant ('Everything under tests/fixtures/ is gitignored ... never committed'), which is the mechanism the team relies on to keep NC data out of git.
  - evidence: `git ls-files | grep philos` returns tests/fixtures/philos/labels/*.csv (3), tests/fixtures/philos/charts/radar_structure_points.geojson, charts/philos_cluster_classification.csv/.png, charts/philos_chart_coverage.png, docs/baselines/*philos*.csv (~10), and tests/replay/test_philos_*_labels.cpp. data/README.md:18 '| philos/ | ... | CC-BY-NC-SA-4.0 | ... |'. Label CSV headers begin '# Video-derived existence/region labels for the philos ... clip'. tests/fixtures/philos/README.md:176 asserts fixtures are 'never committed' — contradicted by the above.
  - verifier: Every factual element of the claim reproduced independently. (1) LICENSE: data/README.md:18 records philos/ as CC-BY-NC-SA-4.0 (NonCommercial+ShareAlike), the same class the project rejected for MOANA/GFW. (2) COMMITTED DERIVATIVES: git ls-files --error-unmatch confirms tests/fixtures/philos/labels/{car_carrier_near,close_approach,sunset_cruise}_labels.csv and tests/fixtures/philos/charts/radar_structure_points.geojson are genuinely tracked; their headers self-identify as philos-derived ('# Video-derived existence/region labels for the philos <clip> clip ... operator video + frame/radar cross-reference pass'). Also tracked: charts/philos_cluster_classification.csv/.png, charts/philos_chart_coverage.png, ~10 docs/baselines/*philos*.csv. (3) FORCE-ADDED: .gitignore:29 '/tests/fixtures/*' ignores everything with explicit ! exceptions ONLY for sim_multisensor and rbad generators — no philos exception, so these were git add -f'd past the ignore rule (commit 6151f50 for car_carrier_near). (4) SELF-CONTRADICTION: tests/fixtures/philos/README.md:175 states 'Everything under tests/fixtures/ is gitignored (local, never committed)' — directly contradicted. (5) TEST CONSUMPTION: PhilosLabelReplay.hpp:368 loads the geojson; test_philos_sunset_labels.cpp:46 and test_philos_close_approach_labels.cpp:40 load the CSVs via loadLabels(). The only nuance is whether bare existence/region labels and extracted coordinates are legally copyrightable adaptations vs. uncopyrightable facts — a genuine edge case — but the project's own stated commercial-usability goal, its explicit prior rejection of NC-SA datasets, and its own README invariant make committing philos-derived artifacts a real license/policy conflict regardless. Matches the task quality bar (license terms incompatible with commercial use, with committed provenance-carrying derivatives).

- **[MEDIUM/unverified]** 683 MB of CC-BY-NC-4.0 (non-commercial) Pohang data present on disk — same license class the project rejected
  - `?:?` (licensing-cleanup, confidence ?)
  - description: data/pohang/ holds 683 MB of the Pohang Canal Dataset (pohang00 + pohang02, each radar.zip/navigation.zip/calibration.zip). data/README.md:195 states the license is 'CC-BY-NC-4.0. Non-commercial only.' This is the identical non-commercial constraint that caused MOANA and GFW to be rejected as commercially-incompatible. The raw zips are not git-tracked (data/ is gitignored, confirmed by `git ls-files | grep pohang` = empty), so this is not a git-history blocker, but keeping non-commercial data staged on disk in a commercially-intended project is a consistency/cleanup risk (it can be inadvertently committed or leaked, and the pohang_radar/pohang_lidar fixture READMEs do not restate the NC restriction).
  - evidence: `du -sh data/pohang` => 683M; data/pohang/pohang00 and pohang02 each contain radar.zip/navigation.zip/calibration.zip. data/README.md:195 'License: CC-BY-NC-4.0. Non-commercial only.' `git ls-files | grep pohang` returns nothing (untracked but present).

- **[MEDIUM/unverified]** Singapore Maritime Dataset has NO recorded license or provenance and is absent from the data manifest
  - `?:?` (missing-provenance, confidence ?)
  - description: The SMD-based fixture (tests/fixtures/smd_eo/) emits 36 bearing/ObjectGT CSVs (on disk under out/) derived from Singapore Maritime Dataset on-shore annotations, fetched via a Google Drive gdown ID and a rarlab unrar binary. Neither the fixture README nor data/README.md records any license for SMD — `grep -niE 'smd|singapore' data/README.md` returns nothing, so SMD is entirely missing from the source-of-truth provenance manifest that claims to cover every source. SMD (Prasad et al., NUS) is a research dataset whose distribution terms are non-commercial/research-oriented and are neither cited nor verified here. Using its derived annotations in the evaluation bundle with zero license provenance is a legal-risk and provenance gap.
  - evidence: `grep -niE 'smd|singapore' data/README.md` => no matches (manifest claims to cover all sources but omits SMD). tests/fixtures/smd_eo/README.md has no license section; fetch is `gdown 1HnHyQzhzzDlYh15y9_K1mNZX3grlSDMM` + rarlab unrar. tests/fixtures/smd_eo/out/ contains ~36 *_ObjectGT.csv derived files on disk.

- **[LOW/unverified]** trondheim_harbor.geojson is OSM/ODbL (share-alike database license) committed to git
  - `?:?` (share-alike-obligation, confidence ?)
  - description: tests/fixtures/autoferry/trondheim_harbor.geojson is tracked in git and its own properties record source 'OpenStreetMap via Overpass API, (c) OpenStreetMap contributors, ODbL'. ODbL is commercial-use-friendly but carries an attribution requirement (satisfied here, in-file) and a share-alike obligation on derivative databases. Bundling an ODbL-derived land polygon into the product is acceptable, but the share-alike term is worth flagging so an integrator does not unknowingly incur database-license obligations. Attribution is present, so this is hygiene, not a blocker.
  - evidence: tests/fixtures/autoferry/trondheim_harbor.geojson properties.source = 'OpenStreetMap via Overpass API, (c) OpenStreetMap contributors, ODbL'; file is in `git ls-files tests/fixtures`.

Coverage notes (licensing-provenance): Cross-checked every dataset root against data/README.md (the stated provenance manifest) and against `git ls-files tests/fixtures data charts`.  CLEAN / commercial-OK (verified): - charts/ NOAA ENC: US5BOSCC.zip/US5BOSCD.zip/US5BOSDC.zip and ENC_ROOT/* are tracked; charts/ENC_ROOT/USERAGREEMENT.TXT confirms NOAA ENCs 'may be downloaded, used, and redistributed without restriction' (US Government product). Redistribution guideline (display agreement + reference) is met by the included USERAGREEMENT.TXT. charts/ANALYSIS.md documents S-57 GDAL parsing provenance. charts/geojson/* are derived from these public-domain ENCs. Clean. - tests/fixtures/static/harbor_obstacles.geojson: tiny hand-authored synthetic ENC-style obstacles (source_id 'ENC:wreck-1'), no external data. Clean. - tests/fixtures/rbad/: only generator/*.py + README + requirements tracked (data local-only); README records Zenodo doi:10.5281/zenodo.16936465, CC-BY-4.0 (commercial-OK). Clean. - tests/fixtures/sim_multisensor/: fully synthetic generator (generator/*.py + README + lockfile tracked); self-produced, no license concern. Clean. - data/dlr (HAXR): 1.4 GB on disk, not tracked; data/README records DOI 10.5281/zenodo.19824555, CC-BY-4.0 (commercial-OK). AIS is anonymised/derived. Note: the tests/fixtures/haxr_cfar/README.md itself carries no license line (provenance lives only in data/README) — minor, not raised as a finding. - data/marinecadastre (US public domain), data/dma (DMA open data / figshare mirror doi:10.6084/m9.figshare.11577543), data/kystverket (NLOD), data/stonesoup (Stone-Soup MIT, LICENSE present; Solent AIS demo) — all commercial-compatible per data/README and confirmed not to be NC. None tracked in git (data/ gitignored). - data/autoferry: CC-BY-4.0 per README; not tracked.  VERIFIED NOT git-tracked (so no git-history licensing blocker): pohang, smd, dlr, dma, kystverket, marinecadastre, stonesoup, autoferry raw data, and the smd_eo/pohang_*/haxr_cfar fixture out/ CSVs — data/ and fixture out/ are gitignored.  Confirmed on-disk sizes: data/pohang 683M (NC), data/dlr 1.4G, data/dma 266M, data/marinecadastre 277M, data/stonesoup 59M, data/kystverket 15M, data/autoferry 7.1M; data/philos and data/smd are empty (cleaned up after extraction) — but philos-DERIVED products are committed (see high finding) and smd-derived CSVs sit in tests/fixtures/smd_eo/out/.  Not inspected in depth: raw HDF5/CSV column-level stats of dlr/dma/marinecadastre (out of the licensing-provenance unit's scope — belongs to the loader/data-quality auditors). .venv/ and site-packages/ under tests/fixtures/philos ignored per instructions.

### unit: philos (3 findings)

- **[HIGH/CONFIRMED]** philos fixtures are CC-BY-NC-SA-4.0 (non-commercial) derivatives, used to validate a commercial product
  - `data/README.md:?` (licensing, confidence ?)
  - description: data/README.md and tests/fixtures/philos/README.md both state the MIT Sea Grant R/V Philos dataset is licensed CC-BY-NC-SA-4.0 (NonCommercial + ShareAlike), form-gated with an 'intended use' question. Every philos fixture the test suite depends on is a derivative of that recording: ownship.csv, ais.csv, radar_plots.csv, camera_bearings.csv (from the bag video+GPS+IMU+AIS+radar) and the labels/*.csv (README: 'Video-derived ... from operator video'). All seven test_philos_* tests validate navtracker against these derivatives. NonCommercial forbids use in support of a commercial product regardless of git redistribution; this exactly matches the project's own established NO-GO precedent on NC data (memory: GFW CC-BY-NC 4.0 CLOSED NO-GO, MOANA rejected; Pohang CC-BY-NC-4.0 'Non-commercial only'). The user org (tkmsgroup.com / ThyssenKrupp Marine Systems) is commercial, making the entire philos validation arm a licensing blocker for a pre-release.
  - evidence: data/README.md provenance table row 'philos/ ... CC-BY-NC-SA-4.0 Form-gated' and section 'MIT Sea Grant R/V Philos (philos/) — NOT FETCHED ... CC-BY-NC-SA-4.0, distributed only through a download form'; tests/fixtures/philos/README.md 'R/V Philos ... MIT Sea Grant'; labels/*.csv headers 'Video-derived existence/region labels ... operator video'.
  - verifier: I independently reproduced every load-bearing observation in the claim.  LICENSE (the core finding): data/README.md line 18 provenance table lists `philos/ ... CC-BY-NC-SA-4.0 ... Form-gated`. The dedicated section (lines 345-368) states verbatim: "CC-BY-NC-SA-4.0, distributed only through a download form ... The form asks for name, email, organisation, and intended use". CC-BY-NC-SA-4.0 carries the NonCommercial + ShareAlike restriction. Confirmed.  DERIVATIVES: tests/fixtures/philos/README.md documents that the extractors turn the R/V Philos `section.bag`/`radar_pcd` (MIT Sea Grant, form-gated) into ais.csv, ownship.csv, radar_plots.csv, camera_bearings.csv. I confirmed the extracted CSVs exist under tests/fixtures/philos/out/ais_ferry_near/ (ais.csv, ownship.csv, radar_plots.csv, camera_bearings.csv, meta.txt, radar_truth.csv). The labels/*.csv headers confirm the "Video-derived ... operator video" provenance: e.g. car_carrier_near_labels.csv reads "# Video-derived existence/region labels ... Produced by the ... operator video + frame/radar cross-reference pass". These are all derivatives of the NC recording.  TEST DEPENDENCE: I found 9 philos test files (tests/replay/test_philos_los_shadow_probe, _occupancy_coverage_6c, _close_approach_labels, _farcross, _sunset_labels, _close_approach_cpa, _ospa, plus tests/benchmark/test_philos_cluttermap_ab, and the PhilosLabelReplay.hpp harness). The claim's "seven test_philos_* tests" undercounts, but that strengthens rather than weakens the finding.  COMMERCIAL CONTEXT: user email is andreas.klots@tkmsgroup.com (ThyssenKrupp Marine Systems, a commercial shipbuilder); this is a pre-release review of the product.  PRECEDENT: the project's own established stance treats NC data as a blocker — data/README.md itself flags Pohang as "CC-BY-NC-4.0. Non-commercial only", and project memory records GFW CC-BY-NC 4.0 CLOSED NO-GO and MOANA rejected on the same grounds. NonCommercial restricts USE (not merely git redistribution), so gitignoring the fixtures does not cure it — the test suite actively consumes the derivatives, and the fixtures are present and live in this main tree.  Adversarial checks for refutation: (1) Is it out of scope? No — "license terms incompatible with commercial use" is explicitly in the auditor's quality bar. (2) Is there a mitigating license grant? None found; the README even instructs requesting the data under an "academic research" pretext, which conflicts with commercial product validation. (3) Count discrepancy (7 vs 9 tests) is immaterial. No basis to refute; the licensing incompatibility is real and well-supported.

- **[MEDIUM/unverified]** OSPA truth is the full AIS report set, but 22 of 23 targets lie far beyond radar coverage
  - `tests/replay/test_philos_ospa.cpp:?` (truth-sensor-misalignment, confidence ?)
  - description: test_philos_ospa.cpp scores OSPA with truth_as_measurement = the entire loadAisCsv() output (runConfig(radar, ais, ...) and runConfig(both, ais, ...)), applying a 100 m cutoff over 1 s windows with no range/coverage filter. In ais_ferry_near only ONE of 23 AIS truth MMSIs (367074170, ~84-92 m) is within the radar's reach; the other 22 sit 1.3-15.8 km away and radar_plots.csv max range is ~976 m, so radar-only config A can never detect them. Every OSPA window therefore accrues ~22 unavoidable misses pinned at the 100 m cap, so the printed 'mean OSPA' is a structural coverage artifact, not a tracking-accuracy figure, and the small real signal (fusion on the one near target) is swamped. The test does not gate on OSPA (only EXPECT_GT unique_ids/n_windows > 0) and comments that it is 'detector- and lifecycle-dominated', but the number is emitted as 'the deliverable' and can mislead anyone comparing it to true-GT datasets.
  - evidence: radar_truth.csv per-uid range: ais_ferry_near = 22/23 targets 1440-15804 m OUT of ~976 m radar coverage, only 367074170 (84-92 m) IN; test_philos_ospa.cpp evalWindow builds truth_now from all truth_as_measurement within +/-0.5 s with ospaGreedy(...,100.0), and runConfig is called with the unfiltered `ais` vector.

- **[LOW/unverified]** Validated camera-bearing fixtures are never exercised by any scenario test
  - `tests/replay/PhilosLabelReplay.hpp:?` (coverage, confidence ?)
  - description: The camera-bearing pipeline has no scenario-level regression coverage. Every philos runClip() call passes load_camera=false (test_philos_occupancy_coverage_6c.cpp lines 178-579, test_philos_los_shadow_probe.cpp lines 246-250), so PhilosLabelReplay's camera path (buildCameraFrames / loadCameraBearingsCsv) is dead in the label-scored tests. sunset_cruise/camera_bearings.csv (4605 rows, README-marked 'center only (if sanity passes)') is consumed by nothing. Only ais_ferry_near/camera_bearings.csv is touched, and solely by the loader smoke test (test_camera_bearing_loader.cpp), which asserts mechanics only (no accuracy, by the stated circularity rule). Separately, car_carrier_near carries _camera_detections.csv but no camera_bearings.csv, so any future load_camera=true on that clip silently no-ops (runClip guards on fileExists). Not a data defect, but the shipped/validated camera bearings have no end-to-end guard.
  - evidence: grep 'runClip(' in test_philos_*.cpp shows no load_camera=true; ls out/sunset_cruise has camera_bearings.csv (unused); ls out/car_carrier_near has _camera_detections.csv but no camera_bearings.csv; PhilosLabelReplay.hpp runClipInputs only loads camera when load_camera && fileExists(camera_csv).

Coverage notes (philos): Cross-checked all four loaders against the data. CLEAN items: (1) Timestamps — every file in every clip is monotonic (loaders also sort by time) and shares one common unix epoch/clock within a clip; ownship/radar/camera/ais start within ~0.3 s and end within ~0.13 s of each other in ais_ferry_near (694.00-713.96 vs 693.94-713.83 vs 694.06-713.92 vs 694.36-713.81), so no silent inter-sensor clock offset. Duplicate timestamps in radar_plots/camera are multiple returns per scan (expected); the loaders handle them. (2) Radar azimuth convention — radar_plots.csv `azimuth_deg` is BODY-relative marine (N=0 CW), which is what loadPlotCsvBodyFrame and loadRadarTruthCsv assume (heading+az). Verified numerically against AIS: target 316028326 heading 239.777 + az 237.44 wraps to 117.2 deg true (computed true bearing 117.4 deg), range 7833 m vs computed 7830 m; near ferry 367074170 heading+6.3=246.1 vs computed 245.9. loadPlotCsv (station frame) would misread these as absolute, but NO philos test uses it. (3) Camera bearing convention — CameraBearingCsvReader reads `bearing_rel_deg` as relative-to-heading, deg->rad, value=atan2(dN,dE); fixtures are consistent (right cam +23..+72, center ~0, left negative); test HeadingCompositionAndValueConvention asserts exactly this math. README documents ais_ferry_near center as AIS-RANSAC validated (residual median 0.45 deg / p90 1.32 deg). (4) close_approach correctly has NO camera_bearings.csv (README: intrinsics REFUSED for the prodromos vessel); no test loads camera from it. (5) Value plausibility — ownship lat/lon in Boston harbor (~42.37,-71.05), heading 0-360; radar range 54-976 m, sig<=0 rows fall back to loader defaults (25 m / 1 deg); AIS sog 0.05-12.6 m/s, cog 0-360, lat/lon in-basin; no NaN/blank in critical columns (finite-value counts equal row counts). (6) radar_truth uid == ais mmsi (derived reprojection). (7) Zero-AIS clips (sunset/close_approach/car_carrier/almost_cross/sailboats) correctly have header-only ais.csv, matching test expectations (farcross asserts almost_cross has no radar_truth). Minor unreported nit: the first radar plot in every clip precedes the first ownship pose by 60-260 ms and is dropped by poseAtOrBefore (negligible, first partial scan). NOT verified: could not open the source .bag/.pcd (data/philos is empty, form-gated) so I could not confirm the CSVs reproduce from the raw recording; extractor scripts (extract_section.py/extract_radar.py/extract_camera_bearings.py) were not line-audited.

### unit: haxr (4 findings)

- **[HIGH/REFUTED]** No license / provenance / attribution recorded for the DLR HAXR dataset that all HAXR tests validate against
  - `data/dlr/manifest.json:?` (license-provenance, confidence ?)
  - description: The HAXR radar+AIS dataset is the primary real-radar validation source for the whole haxr unit (kattwyk/parkhafen/seemannshoeft HDF5+AIS+stations), consumed by test_haxr_ospa, test_los_guard_haxr_ab, and test_veto_isolation_haxr_ab. Its manifest records ONLY per-file SHA256 hashes — no source URL, no license, no citation, no commercial-use terms. There is no LICENSE file anywhere under data/dlr, and no docs/ page documents the dataset's origin or terms. navtracker is a commercial product (tkmsgroup) and the team has explicitly rejected other datasets for non-commercial licensing (memory: D7 MOANA, D10 GFW = CC BY-NC). For the flagship real-radar dataset the commercial-use terms are simply unknown, which is a release-gating question until resolved. (Mitigation: data/dlr and the generated out/ are gitignored, so the data is not distributed with the repo — this is a use/verification blocker, not proven redistribution of restricted data.)
  - evidence: data/dlr/manifest.json contains only {station, split_hour_utc, radar.sha256, ais.sha256} entries and a stations ref — no license/source field (tail + grep -niE 'licen|copyright|cc-by|doi|source' returns nothing). `ls data/dlr/*LICENSE*` = none. grep of docs/ for HAXR provenance/license terms returns nothing.
  - verifier: REFUTED. The claim's core assertions — no license, no source URL, no citation, "commercial-use terms are simply unknown," and "no docs/ page documents the dataset's origin or terms" — are all false. The auditor scoped the license search too narrowly (only data/dlr/manifest.json, `ls data/dlr/*LICENSE*`, and grep of docs/) and missed data/README.md, which sits one directory up and is explicitly the provenance record.  Confirmed sub-observations (auditor is right on these narrow points): data/dlr/manifest.json contains ONLY {station, split_hour_utc, radar.sha256, ais.sha256} entries plus a stations ref — `grep -niE 'licen|copyright|cc-by|doi|source|url|attribut|citation' data/dlr/manifest.json` returns nothing; and there is no data/dlr/*LICENSE* file. But the manifest.json is a hash-integrity file by design, not the provenance record.  The provenance IS recorded, in data/README.md (present in this main tree), which states in its header: "Files are not committed (see .gitignore); this README is the source-of-truth manifest." That README documents HAXR fully: - License table row (line 12): `dlr/` | X-band radar (polar HDF5) + AIS (CSV) | **CC-BY-4.0** | 1.4 GB | Fetched. - Dedicated section (lines 94-135): "## DLR HAXR — Hamburg port X-band radar + AIS (`dlr/`)  DOI: `10.5281/zenodo.19824555`. Published Feb 2026 by DLR Institute of Communications and Navigation (Meinert, Moreira)." with fetch URLs (https://zenodo.org/api/records/19824555/...), sensor-stack/column descriptions, and lineage to the Fowdur/Baum/Heymann 2021 dataset.  Crucially the recorded license is CC-BY-4.0 (attribution-only, commercial-use permitted), NOT a non-commercial license. This is the same README that records the datasets the team rejected for NC terms (pohang CC-BY-NC-4.0, philos CC-BY-NC-SA-4.0), so the distinction is deliberately captured. The claim's premise that HAXR's commercial-use terms are "unknown" and a "release-gating question" is contradicted by the explicit CC-BY-4.0 record.  Note (does not rescue the claim): data/README.md is not git-tracked (`git ls-files --error-unmatch data/README.md` fails; the whole data/ tree is gitignored, though data/.gitignore whitelists README.md via `!README.md`). So provenance is not *distributed with the repo* — but the auditor's own claim already concedes the data is gitignored and frames this as a use/verification blocker, not redistribution. For use/verification, the terms are plainly recorded in the tree alongside the data (CC-BY-4.0, DOI, authors, source URL). The factual claim "no license/provenance/attribution recorded" is wrong.

- **[MEDIUM/unverified]** Flagship real-OSPA-on-real-radar test uses CWD-relative fixture paths and silently GTEST_SKIPs under ctest
  - `tests/replay/test_haxr_ospa.cpp:?` (test-wiring, confidence ?)
  - description: test_haxr_ospa (the only test that streams HAXR plots through the tracker and reports OSPA vs AIS truth) resolves its fixtures via bare relative paths and, unlike the two AB tests, does NOT anchor them with NAVTRACKER_SOURCE_DIR. ctest / gtest_discover_tests run from build/, where those relative paths do not resolve, so fileExists() fails and the test hits GTEST_SKIP. The fixtures are present in the source tree, but the test only actually runs when invoked from the repo root. Result: the primary HAXR accuracy test is effectively never exercised in normal CI, while its companions (test_los_guard_haxr_ab, test_veto_isolation_haxr_ab) DO run because they use srcAbs(NAVTRACKER_SOURCE_DIR). (This overlaps a T3 test-sufficiency finding but is squarely in the haxr scope.)
  - evidence: test_haxr_ospa.cpp:38-41 kDefaultPlotsCsv="tests/fixtures/haxr_cfar/out/kattwyk_08_t40.csv", kAisCsv="data/dlr/kattwyk_08-UTC.csv" (relative); grep -c NAVTRACKER_SOURCE_DIR test_haxr_ospa.cpp = 0. Contrast test_los_guard_haxr_ab.cpp:44 srcAbs()/NAVTRACKER_SOURCE_DIR.

- **[MEDIUM/unverified]** Sparse AIS truth (10-20 s cadence) scored against a 1 Hz eval clock inflates OSPA to the cutoff on most windows
  - `tests/replay/test_haxr_ospa.cpp:?` (truth-eval-misalignment, confidence ?)
  - description: test_haxr_ospa builds one OSPA snapshot per 1 s tick, taking as truth only AIS samples within +/-0.5 s of the tick. But AIS reports arrive every ~10-20 s per vessel, so the large majority of 1 Hz ticks have zero in-window truth. ospaGreedy with an empty truth set and N existing tracks returns the cutoff (n=max(|X|,|Y|), each surplus track costs cutoff^2), i.e. ~200 m for every such window. The reported 'mean OSPA' is therefore dominated by truth-empty windows and measures 'tracks exist when AIS was silent', not tracking accuracy; the EXPECT_LT(mean_ospa,200) guard is nearly vacuous. The truth cadence is native AIS and is fine — the defect is scoring it against a denser clock without hold/resample.
  - evidence: test_haxr_ospa.cpp:106,113-119 (1 s ticks, +/-0.5 s truth gate). AIS per-vessel deltas measured on data/dlr/kattwyk_08-UTC.csv (uid 96443): 10 10 10 20 10 10 10 10 20 10 10 s. Ospa.cpp:13-14 returns max-cardinality cutoff penalty when one set is empty. README.md:61 states AIS cadence 8-12 s.

- **[LOW/unverified]** AIS-derived truth velocity hardcoded to zero for all vessels, including moving ones
  - `adapters/replay/HaxrTruthLoader.cpp:?` (truth-quality, confidence ?)
  - description: loadHaxrTruth sets every TruthSample.velocity to Vector2d::Zero() regardless of vessel motion. The AIS truth is range/azimuth position only, so velocity is simply not carried. This is inert for the current position-only OSPA/GOSPA scoring, but it is a latent misalignment: any velocity-aware truth consumer (velocity-OSPA, motion-model NEES vs truth, or a future SOG/COG accuracy metric) would score moving vessels against a false zero-velocity truth. Worth an explicit comment or a derived velocity so it is not silently mistaken for real ground-truth kinematics.
  - evidence: HaxrTruthLoader.cpp:74 `s.velocity = Eigen::Vector2d::Zero();` for every row.

Coverage notes (haxr): Scope covered: loaders adapters/replay/HaxrTruthLoader.{hpp,cpp} and PlotCsvReplayAdapter.{hpp,cpp}; scenario wiring adapters/benchmark/ReplayScenarioRun.cpp (HaxrScenarioRun); tests tests/replay/test_haxr_ospa.cpp, tests/benchmark/test_los_guard_haxr_ab.cpp, test_veto_isolation_haxr_ab.cpp, test_occupancy_ab.cpp; data tests/fixtures/haxr_cfar/{README.md,.gitignore} and data/dlr/{stations.csv,manifest.json,*-UTC.csv}.\n\nVerified CLEAN:\n- Loader/data column+unit contract matches. Plot CSVs have the 8-column header tod,range_m,azimuth_deg,sigma_r_m,sigma_az_deg,n_cells,amp_max,station exactly as PlotCsvReplayAdapter reads (fields[0,1,2,3,4,7]); AIS CSVs have tod,uid,range(m),azimuth(deg) matching HaxrTruthLoader (fields[0..3]). tod = seconds-of-day UTC (kattwyk_08 AIS starts 28800=08:00:00), confirmed by README.\n- Frame alignment is CORRECT: HaxrTruthLoader offsets truth by station_xy + range*[sin(az),cos(az)] with marine az (N=0, CW); PlotCsvReplayAdapter projects plots via projectRangeBearingToEnu which (Projection.hpp:15) offsets by (range*sinθ, range*cosθ) from the same station position. Truth and plots share one ENU frame per station, so no truth/sensor spatial misalignment.\n- CIRCULARITY RULE HOLDS. Default HAXR path feeds radar plots only and scores vs AIS-derived truth (no AIS as sensor). AIS-as-input is gated behind HAXR_FEED_AIS (off by default); when on (only the veto-isolation test sets it) the test asserts ONLY arm well-formedness, never accuracy-vs-AIS, and the code+test both document the hard rule (ReplayScenarioRun.cpp HaxrScenarioRun comment; test_veto_isolation_haxr_ab.cpp header). los_guard AB does not set HAXR_FEED_AIS (radar-only vs AIS truth).\n- Timestamps: kattwyk_08_t40 and kattwyk_08_dec50_w285 both monotonic non-decreasing after nothing needed (0 out-of-order rows); loaders also std::sort plots and HaxrScenarioRun sorts truth before groupTruth, so the sort-fragmentation trap is handled. AIS truth files monotonic (0 out-of-order).\n- Value ranges plausible: kattwyk plots range 105-2181 m, az 0.07-359.9 deg; parkhafen rmax 2725 m; seemannshoeft rmax 4577 m (per-station radar ranges; AIS rmax slightly larger as README notes AIS-beyond-radar). No NaN/inf in the sampled plot fixtures (grep count 0).\n- Cross-site window alignment for the veto AB is consistent: kattwyk/parkhafen/seemannshoeft *_08_dec50_w285.csv all span tod ~29096..29381 (~284.5 s common window). kattwyk_08_abW285.csv is byte-identical to kattwyk_08_dec50_w285.csv (harmless alias, not a defect).\n- Fixtures are generated (out/ and .venv gitignored) from the DLR HDF5 via extract_plots.py; naming encodes params (t40=amp thr 40, dec50=cluster eps 50 m, w285=285 s window). Generator + validator scripts present.\n\nNot separately reported but noted: HaxrScenarioRun (unlike PhilosScenarioRun) does not resampleTruthToClock, so the los_guard/veto AB metrics are evaluated only at native ~10-20 s AIS instants — a methodology inconsistency vs the philos path, though those tests assert only well-formedness/banded deltas so it does not corrupt a pinned number. test_occupancy_ab.cpp is in scope but does not exercise any HAXR fixture (sim + philos scenarios only).


## Batch D2-autoferry-pohang-rbad-simms

### unit: autoferry (3 findings)

- **[LOW/unverified]** Loader comment claims detections are time-ordered in file; false for 5/9 scenarios, making the stable_sort load-bearing not 'defensive'
  - `/home/andreas/workspace/navtracker/adapters/replay/AutoferryJsonReplay.cpp:?` (timestamp-monotonicity, confidence ?)
  - description: AutoferryJsonReplay.cpp lines 302-308 comment: 'Detections arrive time-ordered in the file, but a defensive stable sort...'. In reality scenario6, scenario13, scenario16, scenario17, scenario22 are NOT time-ordered in the detections JSON (interleaved sensor streams produce hundreds of backward steps). The std::stable_sort by time is therefore REQUIRED for correctness on those files, not merely defensive. No current runtime bug (the sort corrects it and replay stays deterministic on a fixed file), but a maintainer trusting the comment could remove the sort as redundant and feed non-monotonic timestamps to the time-driven engine for 5 of the 9 benchmark scenarios.
  - evidence: python3 over data/autoferry/*: det backward-jump counts scenario6=831 (max backstep 0.208s), scenario16=683 (max 0.711s), scenario13=670 (0.172s), scenario17=466 (0.171s), scenario22=452 (0.204s); scenario2/3/4/5 monotonic (0 backjumps).

- **[LOW/unverified]** Truth dedup keys only on (time,id); scenario5 has one such key carrying two distinct positions, so std::unique silently drops a genuine truth sample
  - `/home/andreas/workspace/navtracker/adapters/replay/AutoferryJsonReplay.cpp:?` (truth-dedup, confidence ?)
  - description: After unifying each ground-truth scan to its max target time (lines 320-337) the loader sorts by (time,id) and removes exact (time,id) duplicates (lines 345-356), comparing ONLY time and truth_id, not position. In scenario5 the key (time=1620116323.2, targetID=1) is produced by two different physical samples whose positions differ (189.98,-437.88) vs (189.87,-438.03), ~0.18 m apart; std::unique keeps the first and discards the second real sample. Impact is negligible at 0.18 m, but the dedup mechanism can silently lose distinct truth if scan-max unification ever collides two genuinely different fixes onto one (time,id).
  - evidence: python3 simulation of the loader's unify+dedup over all scenarios: only scenario5 shows a (time,id) key with conflicting positions (1 occurrence, 0.18 m apart); all other scenarios show 0 conflicts.

- **[LOW/unverified]** Loader comment understates max intra-scan truth-time labelling skew (~0.1s claimed vs 0.4s actual in env-2)
  - `/home/andreas/workspace/navtracker/adapters/replay/AutoferryJsonReplay.cpp:?` (truth-time-skew, confidence ?)
  - description: The comment at AutoferryJsonReplay.cpp lines 310-319 justifies stamping every target in a scan with the scan's max target time by asserting '<=0.1 s labelling skew ~ <1 m at harbour speeds'. The measured maximum intra-scan target-time spread is 0.40 s in the env-2 scenarios (13/16/17), about 4x the stated bound (median spread 0.08-0.12 s). At env-2 harbour speeds the worst-case mis-stamp is ~2 m, still below the env-2 lidar sigma of 3 m, so the conclusion (skew < sensor noise) holds, but the documented numeric bound is wrong.
  - evidence: python3 over data/autoferry/*: intra-scan target time spread max=0.400s (scenario13/16/17), p99=0.200-0.240s, median 0.08-0.12s; env-1 max=0.160s.

Coverage notes (autoferry): Cross-checked loader adapters/replay/AutoferryJsonReplay.{hpp,cpp}, test tests/replay/test_autoferry_json_replay.cpp, and the bench wrapper AutoferryScenarioRun in adapters/benchmark/ReplayScenarioRun.cpp against all 9 data dirs (data/autoferry/scenario{2,3,4,5,6,13,16,17,22}). CLEAN: (1) Schema — every detection object across all 9 files has exactly {sensorID, ownshipPosition(len2), time, measurement, measurements}; every truth object has {targetID, position(len3), time}; field names match loader's find() keys exactly, no renamed/defaulting fields. The extra 'measurements' (plural) field is always [] in all scenarios and is correctly ignored by the loader (loader reads singular 'measurement'). (2) No NaN/Inf in any detection measurement, ownship position, or truth position; no bad values. (3) det count == gt count in every scenario; every gt scan has exactly 2 targets (IDs 1,2), matching the test's Havfruen/Gunnerus (env-1) and Jetboat/Havfruen (env-2) comment. (4) Frame/units VALIDATED against truth: lidar/radar 'measurement' is ownship-relative NED metres — ownship+(n,e) lands 4.1 m median from truth (lidar sc2), radar p10=4.2 m (rest is expected harbor clutter); raw (no ownship add) is ~400 m off, confirming the relative interpretation. IR/EO bearings are NED atan2(e,n): gated residual vs truth = 2.2 deg median (sc2 IR), and both IR and EO gate to true target with small residuals in env-1 and env-2, confirming the documented convention and the pi/2-NED->ENU swap. GT NED->ENU (E,N)=(arr[1],arr[0]) matches loader. (5) Coordinates plausible: positions within ~1 km of Piren datum (63.4389, 10.399); coastline fixture tests/fixtures/autoferry/trondheim_harbor.geojson spans lon 10.382-10.416/lat 63.431-63.446, correctly bracketing the datum. (6) Epoch is unix seconds 2021-05-04/05, plausible. (7) Truth velocities (loader central-difference) well-behaved: only 2 spikes>15 m/s in ~30k samples (both fine-dt jitter, 3.4 m/0.12 s); max sustained target speed 13-16 kn (env) to ~26-31 kn for the fast craft, plausible for the named vessels. (8) After loader unify+dedup, per-target truth is time-monotonic with no surviving duplicate (time,id) except the one 0.18 m scenario5 conflict noted. (9) LICENSING: data/README.md documents autoferry as CC-BY-4.0 (commercial use OK with attribution), with source URL (github Autoferry/sensor_fusion_dataset), paper citation (Helgesen 2022), and a working curl reproduction command matching the checked-in filenames — no licensing blocker, provenance documented. (10) Bench correctly applies env-2 sigma overrides (lidar 3.0, radar 5.0, bearing 0.0925) and Piren datum that the header warns about; all 9 scenarios present, none missing; test's real-scenario2 assertions (measurements>100 all Position2D, truth>100, max_id>=2) hold. NOT independently verifiable here: exact byte-match of checked-in files vs GitHub source (no network), and the precise EO/IR bias-seed magnitudes/signs in the bench (crude nearest-time gating gave IR/EO env-2 bias of -3.8/-1.6 deg vs bench-seeded +4.9/+7.0 deg — likely a convention/tool difference in autoferry_r_calibration.py rather than a data defect; bearing geometry itself validates).

### unit: pohang (8 findings)

- **[HIGH/CONFIRMED]** Pohang Canal Dataset is CC-BY-NC-4.0 (non-commercial) — 683 MB staged on disk in a commercially-intended library, fixture READMEs omit the restriction
  - `data/README.md:?` (licensing, confidence ?)
  - description: data/pohang/ holds the Pohang Canal Dataset (pohang00 + pohang02, each radar.zip / navigation.zip / calibration.zip, plus unzipped pohang00). data/README.md licenses it CC-BY-NC-4.0, 'Non-commercial only.' This is the identical constraint that got MOANA and GFW rejected as commercially incompatible (memory: D7/D10). The data is untracked (data/ gitignored, `git ls-files | grep pohang` = 0), so it is not a git-history blocker, but it is live on disk and can be inadvertently committed/leaked, and the consumer-facing fixture READMEs (tests/fixtures/pohang_radar/README.md, tests/fixtures/pohang_lidar/README.md) never restate the NC restriction, so anyone extracting/using these fixtures has no in-place warning.
  - evidence: data/README.md:13 '| pohang/ | ... | CC-BY-NC-4.0 |' and data/README.md:195 'License: CC-BY-NC-4.0. Non-commercial only.'; `du -sh data/pohang`=683M; `git ls-files data/pohang tests/fixtures/pohang_radar tests/fixtures/pohang_lidar` returns 0 files; neither fixture README mentions NC.
  - verifier: Reproduced every load-bearing observation.  1. License, data/README.md line 13: `| pohang/ | Marine radar (PNG) + GPS + AHRS | CC-BY-NC-4.0 | 395 MB | Fetched |`; line 195: `License: **CC-BY-NC-4.0**. Non-commercial only.`; and the provenance summary (lines 374-375) repeats CC-BY-NC-4.0 for both pohang00 and pohang02. So the non-commercial restriction is unambiguous and stated three times.  2. On-disk size: `du -sh data/pohang` = 683M (larger than the README's stated 395 MB sample, because pohang00 is also unzipped on disk — dir listing shows pohang00/ and pohang02/). Still live on disk.  3. Untracked: `git ls-files data/pohang tests/fixtures/pohang_radar tests/fixtures/pohang_lidar` returns 0 files, so it is gitignored/not in history — not a committed blocker, but present and inadvertently-committable.  4. Fixture READMEs: both tests/fixtures/pohang_radar/README.md and tests/fixtures/pohang_lidar/README.md exist; grep for `licen|CC-BY|non-comm|NC-4|commerc` returns nothing in either — neither restates the NC restriction.  The license terms (CC-BY-NC-4.0, non-commercial only) are the identical class of restriction that memory records as having gotten D7 (MOANA) and D10 (GFW) rejected as commercially incompatible; the user's org (tkmsgroup.com) is a commercial marine-systems vendor and the library is consumer/commercially-oriented (integration guide, "library consumers"). This is an in-scope license/provenance finding: a non-commercial-licensed dataset staged live on disk in a commercially-intended library with no in-place NC warning in the consumer-facing fixture READMEs. This is a documentation/provenance finding, not data corruption, but the task brief explicitly counts "license terms incompatible with commercial use" as reportable. Claim confirmed.

- **[MEDIUM/unverified]** extract_ownship.py reads the AHRS quaternion in the wrong component order (assumes w,x,y,z; data is x,y,z,w) → heading_deg_quat/roll/pitch are physically impossible
  - `tests/fixtures/pohang_radar/extract_ownship.py:?` (units-frame, confidence ?)
  - description: quat_to_euler_deg treats ahrs.txt columns 2-5 as (q0=w,q1=x,q2=y,q3=z), but the Pohang ahrs.txt quaternion is stored (x,y,z,w) — the 4th component is the scalar. This makes every derived attitude wrong: the produced pohang00_ownship.csv has roll spanning the full -179.96..179.99 deg and pitch pinned negative (-8.44..-0.96 deg) for a boat transiting a calm canal, both physically impossible. A downstream harness that picks heading_deg_quat (the README explicitly offers it as an alternative to the GPS heading) or the roll/pitch columns would be fed garbage attitude.
  - evidence: tests/fixtures/pohang_radar/extract_ownship.py:32-45 assumes (w,x,y,z). First ahrs row = (-0.00816, 0.02164, -0.61902, 0.78504); norm=1.0. Recomputed yaw with script order (w,x,y,z)=182.27 deg (matches produced heading_deg_quat=-177.66≡182.34), with correct order (x,y,z,w)=283.46 deg which matches gps heading col-7 270.18 within a plausible mount offset. Stats on out/pohang00_ownship.csv: roll min/max -179.96/179.99, pitch min/max -8.44/-0.96.

- **[MEDIUM/unverified]** Generated pohang ownship CSV header (lat_deg/lon_deg/heading_deg_gps) is unrecognized by the only ownship loader — loadOwnshipCsv returns EMPTY silently
  - `adapters/replay/OwnshipCsvReader.cpp:?` (loader-mismatch, confidence ?)
  - description: OwnshipCsvReader::loadOwnshipCsv matches header names lat/latitude, lon/longitude, heading_deg/heading. The pohang extractor emits lat_deg, lon_deg, heading_deg_gps, heading_deg_quat — none of which match lat/lon/heading_deg. Because c_lat and c_lon stay -1, the loader hits `if (c_time<0||c_lat<0||c_lon<0) return out;` and returns an empty pose vector with no error. Any pipeline that wires the pohang ownship fixture through the documented replay path (loadOwnshipCsv → feedOwnshipHistory → RadarTruthCsvReader/PlotCsvReplayAdapter) would get zero poses, and then RadarTruthCsvReader drops every row (poseAtOrBefore fails) — a silent total loss disguised as 'no data'. The philos fixture works only because it uses the bare names lat/lon/heading_deg.
  - evidence: adapters/replay/OwnshipCsvReader.cpp:51-56 (exact match set) and :56 early return; head of tests/fixtures/pohang_radar/out/pohang00_ownship.csv = 'unix_time,lat_deg,lon_deg,sog_mps,cog_deg,heading_deg_gps,heading_deg_quat,roll_deg,pitch_deg'; contrast philos header 'unix_time,lat,lon,heading_deg'.

- **[MEDIUM/unverified]** LiDAR raw source is absent and deleted-by-design — out/pohang00_lidar_port_plots.csv cannot be regenerated or verified
  - `tests/fixtures/pohang_lidar/README.md:?` (provenance, confidence ?)
  - description: The pohang_lidar fixture's checked-out product (out/pohang00_lidar_port_plots.csv, 5946 plots) has no reproducible source in the tree: data/pohang/pohang00/lidar/ is an empty directory, there is no lidar_port.zip and no .bin/.pcd anywhere under data/pohang, and the README's documented workflow ends with `rm -f $LIDAR` (delete the 12 GB source after extraction). So the generator cannot reproduce the fixture, and there is no way to audit the DBSCAN parameters, clock, or intensity handling against ground input. This is the provenance-broken failure mode: a data artifact whose inputs are gone.
  - evidence: `ls -la data/pohang/pohang00/lidar/` = empty (only . and ..); `find data/pohang -iname '*lidar*' -o -iname '*.bin'` finds only the empty dir; tests/fixtures/pohang_lidar/README.md:59-61 documents `rm -f $LIDAR`; out CSV present with 5946 rows.

- **[MEDIUM/unverified]** Radar extractor emits continuous canal-wall returns as single 'plots' with physically meaningless sigmas (sigma_az up to 94.7 deg, sigma_r up to 658 m)
  - `tests/fixtures/pohang_radar/out/pohang00_plots.csv:?` (data-quality, confidence ?)
  - description: pohang00_plots.csv treats each connected blob as one point plot, so the long continuous canal-wall returns (README: largest blob 300,950 px) collapse into single rows carrying sigma_az up to 94.7 deg and sigma_r up to 657.7 m. A 95-degree azimuth 1-sigma on a single point measurement is not a plausible sensor uncertainty; it is a whole-arc extended return mislabeled as a point. Any OSPA/tracker evaluation that ingests these as point measurements (the whole purpose of the plots-CSV contract) would be badly misled — the gate/likelihood math assumes point returns with real covariance.
  - evidence: Stats on tests/fixtures/pohang_radar/out/pohang00_plots.csv (273,752 rows): range 1.35..3001.1 m (capped at --max-range-m 3000), sigma_r_m min/max 0.004/657.745, sigma_az_deg min/max 0.0/94.749; README 'Largest single blob 300950 pixels — canal-wall continuous return'.

- **[LOW/unverified]** 24% of LiDAR plots have amp_max=0 (and one has range 0.0) — degenerate rows in the 'near-perfect detector' reference
  - `tests/fixtures/pohang_lidar/out/pohang00_lidar_port_plots.csv:?` (data-quality, confidence ?)
  - description: 1412 of 5946 rows in the lidar plots CSV report amp_max=0, which the README says 'carries the cluster's peak intensity'; a zero peak intensity for a real Velodyne cluster is suspect (either an intensity-extraction gap or genuinely-empty returns), and the fixture is specifically pitched as a clean reference detector. One row also has range_m=0.0 (a cluster on/through the sensor origin). These degenerate rows undercut the 'essentially noise-free' claim used to attribute all downstream track defects to the tracker.
  - evidence: `awk -F, 'NR>1&&$7==0'` on tests/fixtures/pohang_lidar/out/pohang00_lidar_port_plots.csv = 1412 of 5946; python stats: range min 0.0 (1 nonpositive), sigma_az up to 72.8 deg; README claims amp_max is peak intensity and the stream is near-noise-free.

- **[LOW/unverified]** pohang00_plots.csv and pohang00_full.csv are byte-identical duplicates (~18 MB each)
  - `tests/fixtures/pohang_radar/out/pohang00_full.csv:?` (hygiene, confidence ?)
  - description: Two of the four radar out/ files are the exact same content, wasting ~18 MB and creating ambiguity about which is the canonical plots CSV. pohang00_debug.csv is a smaller subset (9648 rows) of the same schema.
  - evidence: md5sum tests/fixtures/pohang_radar/out/pohang00_plots.csv and pohang00_full.csv both = 55b37f5baf6c2fe3f98dec4df48f07b1; both 18,418,597 bytes; pohang00_debug.csv wc -l = 9648.

- **[LOW/unverified]** 21 duplicate timestamps in the ownship CSV from nearest-neighbor merge; not deduped by the loader
  - `tests/fixtures/pohang_radar/out/pohang00_ownship.csv:?` (timestamps, confidence ?)
  - description: pohang00_ownship.csv contains 21 rows with timestamps equal to a neighbor (from pd.merge_asof over ~11k GPS rows). loadOwnshipCsv sorts but does not dedupe, so OwnShipProvider receives multiple poses at the same instant; poseAtOrBefore then resolves ties by whatever std::sort leaves last. Low impact (poses at equal t are near-identical) but it is a nondeterminism-adjacent hygiene issue for a system whose replay must be deterministic.
  - evidence: python check on tests/fixtures/pohang_radar/out/pohang00_ownship.csv: 11033 rows, time monotonic True, duplicate-timestamp count 21, span 2217.999 s; OwnshipCsvReader.cpp:69-70 sorts only, no dedupe.

Coverage notes (pohang): SCOPE COVERED: adapters/replay/RadarTruthCsvReader.{cpp,hpp}, adapters/replay/OwnshipCsvReader.cpp, adapters/replay/PlotCsvReplayAdapter.hpp (pohang refs), tests/replay/test_radar_truth_loader.cpp; tests/fixtures/pohang_radar/{README.md,extract_ownship.py,out/*}, tests/fixtures/pohang_lidar/{README.md,out/*}; data/pohang/{pohang00,pohang02} raw. grep confirmed NO C++ test/bench/tool consumes pohang — the RadarTruthCsvReader test runs against philos fixtures (lat/lon/heading_deg header, epoch ~2022), not pohang; all pohang fixtures + data are untracked/gitignored, staged only on disk.  CROSS-CHECKS DONE & CLEAN: Coordinates plausible for Pohang — gps.txt lat 36.0236..36.0504, lon 129.3703..129.3888 (matches ~36.0N,129.4E). Radar/LiDAR clocks ARE aligned: radar first tod 1625124349.321661 (from radar/timestamp.txt), lidar first tod 1625124349.220652 — 0.10 s apart, same Unix epoch (2021-07-01), both spanning ~2218 s. tod is Unix epoch seconds as the READMEs state (verified radar plots tod == timestamp.txt values). Timestamps MONOTONIC after loader sort in all out CSVs (radar plots 273752 rows mono; lidar 5946 rows mono; ownship 11033 rows mono). No non-positive ranges in radar (min 1.35 m, capped at 3000 m as designed); RadarTruthCsvReader correctly drops range<=0 and uid==0. Body-frame azimuth 0..360 deg consistent with the marine-convention rotation in RadarTruthCsvReader.cpp:70-71 (world_bearing = heading+az) matching PlotCsvReplayAdapter. gps SOG 4.46..18.52 m/s (~9..36 kn) — high but plausible for a fast transit; extract_ownship maps col-10 as sog_mps. quaternion norm ~1.0 (valid unit quaternions; only the component-order interpretation is wrong). NOT verified (out of read-only scope / absent): actual PNG->range_m pixel scale (README flags --max-range-m 3000 as an unresolved unknown), and lidar raw packing (source data absent).

### unit: rbad (1 findings)

- **[LOW/unverified]** Substantive R-BAD tests silently skip under default ctest cwd (only label test runs)
  - `?:?` (coverage-visibility, confidence ?)
  - description: The default fixture root is RBAD_DIR="tests/fixtures/rbad" resolved from the process cwd (RbadScenarioRun.cpp:119). Under ctest the cwd is the build directory, so the relative path does not resolve, generate() returns an empty Scenario, and the three fixtures-dependent tests (GenerateCarriesDatumAndReferenceTracks, MhtConsistencyReportVsReferenceTracker, DeterministicReplay) all GTEST_SKIP. Only BatteryLabels (which needs no fixtures) actually runs. A 'green' R-BAD suite under plain ctest therefore validates nothing about the data path unless RBAD_DIR is exported to an absolute path. Given this project's documented history of silent fixture-skips masking a red master (feedback_verify_skip_list), the skip should be surfaced, not silent.
  - evidence: RbadScenarioRun.cpp:119 builds dir from envOr("RBAD_DIR","tests/fixtures/rbad") + label; test_rbad_scenario_run.cpp lines 90/121/134 all GTEST_SKIP on empty measurements. Only BatteryLabels has no fixture dependency.

Coverage notes (rbad): Read loader (adapters/benchmark/RbadScenarioRun.cpp/.hpp), the plot-CSV parser it delegates to (adapters/replay/PlotCsvReplayAdapter.cpp loadPlotCsv), the test (tests/benchmark/test_rbad_scenario_run.cpp), and all generator/provenance files (generator/extract_rbad.py, README.md, requirements.txt, CHECKSUMS.txt).\n\nLOADER vs DATA cross-check (all 6 scenarios kalimnos_16/17/3, kos_11/16/5): loadPlotCsv reads field[0]=tod,1=range_m,2=azimuth_deg(marine N=0 CW),3=sigma_r_m,4=sigma_az_deg,7=station and needs >=8 fields; fixtures have 10 cols with station='rbad' at field[7]. Confirmed via python: all rows 10 cols, single station 'rbad'. tod monotonic non-decreasing in every plot AND reference file; typical dt=1.0s (1 Hz labelled sub-sample). No NaN/inf in any numeric column. Ranges 3.28-55.8 m across the battery (kos_5 max 36.6) - all well under the test's <200 m body-frame bound and consistent with the README's ~56 m short-range claim; range>0 so none are dropped by the loader's range<=0 skip. Azimuth within sensor FOV (-64..+52 deg). sigma_r 0.01-4.0 m, sigma_az 0-21.6 deg (per-plot cloud spread, capped 30 in generator; 0 values fall back to loader defaults 25m/1deg).\n\nFrame consistency: generator computes az=atan2(x,y) (marine) and east_m=x/north_m=y; loader re-projects range/az to ENU E=range*sin(az)=x, N=range*cos(az)=y - verified it round-trips (refs first row 1.32/3.52 == projection of plot range 3.7594/az 20.556). Plots and reference_tracks are 1:1 by tod multiset in all 6 scenarios (Counter equal). No datum conversion needed (body-frame, no geo reference) - matches ADR/README statement that anchored-static logic is untestable here.\n\nGenerator reproducibility: ran extract_rbad.py --out-root <scratchpad> (read from present _download/labelled logs; all 6 source logs exist) - regenerated all 18 files diff-identical to committed, and sha256sum of committed radar_plots/reference_tracks/meta matches CHECKSUMS.txt exactly for all 18. Determinism contract holds.\n\nLabel-misuse check (the key R-BAD hazard): CLEAN. test only asserts total_our_tracks>0; it deliberately does NOT report/assert RMSE or localization, computeMetrics(result,{}) uses default MetricsParams (truth is carried in BenchResult from the loaded reference tracks) and only continuity/cardinality consistency numbers are printed. Comments in loader, header, README, and generator uniformly label the trajectories as authors' reference-tracker output (cross-tracker consistency, not accuracy). Dock_Label is binary {0,1} everywhere and asserted-on nowhere.\n\nLicense/provenance: CC-BY-4.0 (Zenodo 16936465, doi:10.5281/zenodo.16936465, MDPI Electronics 14(20):4065) - commercial-use compatible, attribution present in README + every meta.txt. No NC/GPL blocker (unlike prior D7/D10 rejects).\n\nAbsent-raw check: no code/test/generator references the undownloaded 31.6 GB raw archive; only README/docstring mention it as a deferred manual-label route. Git hygiene good: only README/extract_rbad.py/requirements.txt tracked; rbad_*/ and _download/ correctly gitignored (git check-ignore confirms). .venv grep hits ignored per instructions.

### unit: sim-multisensor (3 findings)

- **[MEDIUM/unverified]** README claims range-dependent Pd and GPS/heading faults are exercised, but no committed fixture enables them
  - `tests/fixtures/sim_multisensor/README.md:?` (doc-data-mismatch, confidence ?)
  - description: tests/fixtures/sim_multisensor/README.md ("Anti-model-matched-optimism" section) states: "Range-dependent Pd, position quantization, M.1371 cadence, GPS/heading faults — all present, none assumed away." Only position quantization and M.1371 cadence are actually present (they are always-on in sensors.py). Range-dependent Pd, own-ship heading faults, and own-ship staleness gaps are implemented but disabled in every one of the 6 committed sim_ms scenarios. These features are gated behind spec.radar_range_dependent_pd (sensors.py ~L54), spec.ownship_heading_fault (~L195), and spec.ownship_stale_gap (~L186), and no scenario in scenarios.py sets any of them. An evaluator reading the README will over-trust what the fusion gate stresses.
  - evidence: meta.txt for all 6 sim_ms scenarios show `ownship_stale_gap: None` and `ownship_heading_fault: None`; `grep -c radar_range_dependent_pd=True generator/scenarios.py` = 0; sensors.py L54-55/L186-188/L195-197 gate these effects on those (unset) flags; README.md line ~113-114 asserts they are all present.

- **[LOW/unverified]** Detection table advertises an EoIr camera sensor for 5 scenarios that have no camera data
  - `adapters/benchmark/SimMultisensorScenarioRun.cpp:?` (config-inconsistency, confidence ?)
  - description: SimMultisensorScenarioRun::descriptor() appends an EoIr (Bearing2D, P_D=0.85, 8 km) entry whenever with_camera_ is true, which is the default for all 6 sim_ms scenarios. But only sim_ms_anchored_camera actually emits camera bearings; the other five (crossing, headon, overtaking, ais_dropout, clutter_burst) have header-only camera_bearings.csv (1 line). This contradicts the code's own rationale for the Imazu family (lines 124-127), which omits EoIr precisely so that "a phantom EoIr sensor would model misdetections for a sensor that isn't there" cannot happen. I confirmed the entry is currently INERT: the per-scan miss penalty in TrackTree::branch iterates only over sensors present in the scan (TrackTree.cpp L178 `for (const Measurement& z : scan)`), and EoIr never appears in a scan for these five, so no phantom miss/clutter/birth is charged. It is a misleading descriptor, not a metric bug — but it makes test_sim_multisensor_scenario_run.cpp's `detection_table.size() >= 2` pass with a third sensor that contributes nothing.
  - evidence: adapters/benchmark/SimMultisensorScenarioRun.cpp L128-131 add EoIr under with_camera_ (default true); camera line counts: only sim_ms_anchored_camera=284, other five=1 (header only); TrackTree.cpp L166-186 shows miss penalty iterates scan measurements only, so EoIr is never charged when absent.

- **[LOW/unverified]** README CSV-schema note that loadAisCsv drops SOG/COG/nav_status is stale
  - `tests/fixtures/sim_multisensor/README.md:?` (stale-doc, confidence ?)
  - description: README.md (CSV schemas > Notes) says: "loadAisCsv currently reads only time/mmsi/lat/lon and drops sog_mps/cog_deg/nav_status (the #20 velocity-path gap)." This is no longer true: AisCsvReplayAdapter.cpp now has an emit_velocity path (detectColumns picks up sog_mps/cog_deg/nav_status, L69-75) that produces PositionVelocity2D and surfaces nav_status hints, and SimMultisensorScenarioRun.cpp wires it via the SIMMS_AIS_VELOCITY env flag (L164, L174). The default remains Position2D (byte-identical), so the gate still defaults to dropping velocity, but the flat claim that the loader cannot read those columns is incorrect and could mislead someone assessing what the fixtures' extra columns are for.
  - evidence: README.md lines 91-94; AisCsvReplayAdapter.cpp L120-214 (emit_velocity, sog/cog/nav_status parsing); SimMultisensorScenarioRun.cpp L163-164, L174 (ais_velocity from SIMMS_AIS_VELOCITY passed to loadAisCsv).

Coverage notes (sim-multisensor): Cross-checked all loader code against the data. LOADERS READ: adapters/benchmark/SimMultisensorScenarioRun.cpp (+.hpp), tests/benchmark/test_sim_multisensor_scenario_run.cpp, and the four replay loaders it calls (OwnshipCsvReader, AisCsvReplayAdapter, PlotCsvReplayAdapter::loadPlotCsvBodyFrame, CameraBearingCsvReader). GENERATOR READ in full: geo.py, truth.py, sensors.py, clutter.py, scenarios.py, writer.py, generate.py. Also read core/tracking/SensorDetectionModels.hpp, core/tracking/TrackTree.cpp (miss branch), core/benchmark/Sweep.cpp (detectionModelFor) to determine detection-table impact.\n\nVERIFIED CLEAN: (1) truth.csv for all 6 sim_ms scenarios + all 22 imazu scenarios — exactly 1.0 s dt, strictly monotonic per truth_id, no duplicate (time,truth_id) keys, positions all within Trondheimsfjord bounds (63-64N,10-11E), no speeds >20 m/s, shared integer-second clock across vessels (max cardinality 2-3 as tests expect). (2) Schema/column order + units of every CSV match each loader exactly (unix_time epoch seconds, marine deg N=0 CW, SI m/m·s, AIS sog already m/s). (3) CHECKSUMS.txt — recomputed sha256 of all 140 committed CSVs (30 sim_ms + 110 imazu), zero mismatches, no on-disk CSV missing from the manifest. (4) Frame/projection alignment: reconstructed radar body-frame plots through own-ship heading composition and AIS lat/lon through the shared geo::Datum — at t=0 headon the two real radar detections land 54 m / 146 m from truth (consistent with sigma_r=18 m + 0.7 deg cross-range at 3.5-4.6 km), clutter plots land 2.2-3.3 km away as expected; AIS positions land 0.6-1.6 m from truth (position quantization only). (5) Designed features present: AIS dropout gap for mmsi 257000401 is 197.2->383.2 s (spec 200-380); clutter_burst averages 39 plots/scan in-window vs 14 out (compound-K + burst); crossing give-way shows SOG 5.84-7.3 m/s speed loss during its out-of-model turn; anchored (257000601) and camera-only (257000602) both persist in truth as tests require. (6) Dependency pins present (trafficgen==0.9.0, numpy==2.5.1) with a full lock file. NOT DONE: did not regenerate fixtures from the generator (would require the git-ignored .venv + DNV trafficgen and would write files); regenerability is only checksum-self-consistent, not independently reproduced. Scope note: imazu_* dirs live under tests/fixtures/sim_multisensor and were structurally spot-checked (clean), but the deep audit focused on the 6 trafficgen sim_ms scenarios per the task hints.


## Batch D3-geomisc-emitters-baselines

### unit: geo-misc (3 findings)

- **[HIGH/CONFIRMED]** Singapore Maritime Dataset has no recorded license/provenance and is absent from the source-of-truth manifest
  - `?:?` (licensing-provenance, confidence ?)
  - description: The smd_eo fixture (in-tree, my scope) is built from the Singapore Maritime Dataset on-shore annotations, fetched by its README via a Google Drive gdown ID (1HnHyQzhzzDlYh15y9_K1mNZX3grlSDMM) plus a rarlab unrar binary. data/README.md is explicitly the 'source-of-truth manifest' and lists 8 sources with per-source license (dlr CC-BY-4.0, marinecadastre US-public, dma DMA-open, kystverket NLOD, philos CC-BY-NC-SA-4.0, etc.) — but SMD/Singapore appears NOWHERE in it, and tests/fixtures/smd_eo/README.md has no license section at all. SMD (Prasad et al., NUS) is a research dataset whose commercial-use terms are not established anywhere in this tree. navtracker is a commercial product (tkmsgroup) and the team has explicitly rejected other datasets on non-commercial grounds (pohang CC-BY-NC, philos CC-BY-NC-SA, D7 MOANA, D10 GFW). The commercial-use permission for SMD is therefore an unresolved release-gating question. Mitigations that lower the ACTIVE risk (but not the provenance gap): data/smd is empty, the derived out/*.csv are gitignored (not distributed with the repo), and no test/bench/adapter currently consumes the fixture (grep of adapters/tests/core/app for smd_eo/data/smd/ObjectGT = NONE).
  - evidence: `grep -niE 'smd|singapore' data/README.md` => NONE (manifest omits it while claiming to cover every source). `grep -niE 'licen|copyright|terms' tests/fixtures/smd_eo/README.md` => NONE. README fetch step: `.venv/bin/gdown 1HnHyQzhzzDlYh15y9_K1mNZX3grlSDMM`. No cpp/hpp under adapters|tests|core|app references smd_eo/data/smd/ObjectGT.
  - verifier: Every load-bearing observation reproduces verbatim:  1. Manifest omits SMD: `grep -niE 'smd|singapore' data/README.md` => exit 1 (no matches). data/README.md line 6 self-describes as "this README is the source-of-truth manifest," and its table lists 8 sources each with a License column (dlr CC-BY-4.0, pohang CC-BY-NC-4.0, marinecadastre US-public, dma DMA-open, stonesoup MIT, kystverket NLOD, philos CC-BY-NC-SA-4.0, autoferry CC-BY-4.0). Singapore/SMD is absent.  2. Fixture README has no license: `grep -niE 'licen|copyright|terms' tests/fixtures/smd_eo/README.md` => exit 1 (no matches). The README ("Singapore Maritime Dataset annotation-replay fixture") documents inputs, emitted columns, and fetch steps but records no license/provenance/commercial-use terms for SMD (Prasad et al., NUS).  3. Fetch mechanism confirmed: README line ~72 uses `.venv/bin/gdown 1HnHyQzhzzDlYh15y9_K1mNZX3grlSDMM -O $SMD/SMD_onshore.zip` plus a rarlab unrar binary — exactly as claimed.  4. No consumer: grep for `smd_eo|data/smd|ObjectGT|SMD_onshore` across adapters/tests/core/app --include=*.cpp/*.hpp/*.h => exit 1 (none). All references are inside the fixture's own Python scripts/README. data/smd/ is empty (ls shows no files).  Additional finding that STRENGTHENS the "not actively distributed" mitigation but does NOT close the provenance gap: `git check-ignore` shows the entire fixture is gitignored via `.gitignore:29 /tests/fixtures/*`, and `git ls-files | grep -i smd` => none. So no SMD artifact (not even README/scripts/out CSVs) is committed to the repo. Active distribution risk is therefore low.  However the claim's core assertion — a research dataset with no recorded license/provenance, omitted from a doc that explicitly claims to be the source-of-truth manifest, in a commercial product (tkmsgroup) whose team has rejected other datasets on non-commercial grounds — is factually accurate and reproduced. This falls squarely in the accepted quality-bar category "missing provenance/license." The observations are correct; the auditor did not misread the data or loader.

- **[LOW/unverified]** ~558 MB of fetched AIS data is unused by any test, tool, or bench (release hygiene)
  - `?:?` (unused-data, confidence ?)
  - description: Of the data/ sources in my scope, only data/dlr is actually consumed (HAXR kattwyk_08 is read by tests/replay/test_haxr_ospa.cpp, test_los_guard_haxr_ab.cpp, test_veto_isolation_haxr_ab.cpp, and adapters/benchmark/ReplayScenarioRun.cpp via loadHaxrTruth/loadStations). data/dma (266 MB, two .7z), data/kystverket (15 MB, 5 GeoJSON snapshots), and data/marinecadastre (277 MB zip) are fetched and documented but not referenced by any C++/Python loader, test, or bench. data/stonesoup/Stone-Soup is a full 56 MB framework clone (incl. a build/ artifact dir) that the crosscheck tool does not import from (it uses the pip-installed stonesoup package). This matches docs/algorithms/comparison-baselines.md ('~2.3 GB of fetched data unused'; D3 MarineCadastre/DMA and D6 Kystverket are 'open/unscheduled'). Documented as intentional data-expansion staging, so this is hygiene, not a defect — but a reviewer should know these sets carry no coverage today.
  - evidence: `grep -rln 'marinecadastre|kystverket|data/dma|dk_csv|AIS_2024|realtime_geojson' --include=*.cpp --include=*.hpp --include=*.py adapters tests core app tools` (excluding docs) => NONE. Only data/dlr paths appear (test_haxr_ospa.cpp:40-41, ReplayScenarioRun.cpp:40-41). data/stonesoup only referenced in docs.

- **[LOW/unverified]** Documented AIS/stations column schema does not match the actual checked-in file headers
  - `?:?` (schema-doc-mismatch, confidence ?)
  - description: data/README.md documents the DLR AIS columns as `tod, uid, range_m, azimuth_deg` (lines 108-110) and stations as `station, x_m, y_m` (line 111-112), and adapters/replay/PlotCsvReplayAdapter.hpp doc (lines 18,23) states `station,x_m,y_m`. The actual files use different header text: data/dlr/kattwyk_08-UTC.csv header is `tod,uid,range (meters),azimuth (degrees)` and data/dlr/stations.csv header is `station,x (meters),y (meters)`. No functional break — both loaders (HaxrTruthLoader.cpp:61-64, PlotCsvReplayAdapter.cpp:56-57) parse strictly by column POSITION and skip the header line — but the documented schema would mislead anyone writing a new consumer that keys by column name. Data values themselves are sane (see coverage_notes).
  - evidence: `head -1 data/dlr/kattwyk_08-UTC.csv` => `tod,uid,range (meters),azimuth (degrees)`; `head -1 data/dlr/stations.csv` => `station,x (meters),y (meters)`. data/README.md:108-112 and PlotCsvReplayAdapter.hpp:18,23 say `range_m/azimuth_deg` and `x_m/y_m`. Loaders index fields[0..3]/fields[0..2], no name lookup.

Coverage notes (geo-misc): LOADERS (all read, cross-checked against data): (1) adapters/static/GeoJsonStaticObstacles.cpp — correct GeoJSON [lon,lat] order (coords[0]=lon, coords[1]=lat), WGS84 range guards, negative-radius clamp, malformed-JSON→runtime_error, per-feature skip; well covered by tests/adapters/static/test_geojson_static_obstacles.cpp (18 cases). tests/fixtures/static/harbor_obstacles.geojson: 2 valid Point features, Boston harbor coords (-71.05,42.35) in correct [lon,lat] order, sane depth/radii; LoadsFixtureFromDisk asserts size==2 (matches). CLEAN. (2) adapters/land/GeoJsonCoastline.cpp — correct [lon,lat] ring parse; no in-scope coastline fixture to cross-check (it loads autoferry/trondheim_harbor.geojson, out of scope). (3) tools/stonesoup_gospa_crosscheck.py — assumptions verified against the checked-in data/stonesoup/Stone-Soup clone: alpha hardcoded=2 (ospametric.py:87), switching_penalty default 0.0 (line 71), compute_gospa_metric returns dict with distance/localisation/missed/false keys, dummy_cost=c^p/alpha (line 350) matches the tool's dummy=c^p/2.0. Tool imports pip stonesoup (not the clone) but the clone confirms API consistency; MIT LICENSE present. CLEAN. DATA CROSS-CHECKS: data/dlr/kattwyk_08-UTC.csv — 14794 rows, tod strictly monotonic (0 non-monotonic), tod=28800..32396 (=08:00 UTC seconds-of-day, consistent with hour-08 chunk), range 110-3309 m, az 0.08-359.85 deg — all plausible; HaxrTruthLoader treats az as compass bearing (east=r*sin, north=r*cos), correct. data/dlr/stations.csv 13 stations parse cleanly. data/dlr/manifest.json lists 13 stations x 3 hours with sha256 per file; only 3 stations (kattwyk/parkhafen/seemannshoeft) x 3 hours downloaded — a documented partial sample, not an error. data/dlr license CC-BY-4.0 (commercial-OK), marinecadastre US-public, dma DMA-open, kystverket NLOD, stonesoup MIT — all commercial-compatible per data/README.md. NOTES NOT ELEVATED: smd_eo out/ (gitignored, not checked in) contains MVI_1452.bearings.csv byte-identical (same md5) to MVI_1452_VIS_Haze_ObjectGT.csv — generator wrote duplicate content to two names, but it's gitignored generated output, no test consumes it; extract_bearings.py needs the 3GB SMD archive (absent, data/smd empty) to regenerate, but out/ is not a checked-in fixture so nothing to reproduce. parseCoastlineGeoJson calls nlohmann::json::parse without the try/catch that the static loader has (malformed JSON leaks a raw nlohmann exception rather than runtime_error) — code robustness, not a data issue, and no in-scope fixture exercises it.

### unit: sim-emitters (5 findings)

- **[MEDIUM/unverified]** AIS emitter injects one pos_std_m for all targets, decoupled from the per-target high_accuracy covariance the adapter stamps
  - `sim/AisEmitter.cpp:?` (noise-covariance-mismatch, confidence ?)
  - description: AisEmitter draws isotropic position noise with a single std cfg_.pos_std_m for every target (sim/AisEmitter.cpp:15 constructs noise_(0, pos_std_m); lines 60-61 draw nx/ny from it regardless of te.high_accuracy). The consuming AisAdapter, however, stamps the Measurement covariance from the per-target high_accuracy flag: sigma = high_accuracy ? position_std_high_accuracy_m(10m) : position_std_standard_m(30m) (adapters/ais/AisAdapter.cpp:25-26, cov = I*sigma^2 line 68). So a target with high_accuracy=false receives 10 m of injected noise but a 30 m (9x-too-large) stamped covariance. The high-accuracy case matches only because two independent default knobs coincide (pos_std_m=10 == position_std_high_accuracy_m=10); changing pos_std_m without touching the adapter, or emitting any standard-accuracy target, silently breaks the noise/covariance identity. Any future NEES/consistency test built on the AIS bus channel would be biased with no visible failure.
  - evidence: sim/AisEmitter.cpp:15,60-61 (single pos_std_m noise, no high_accuracy branch); adapters/ais/AisAdapter.cpp:25-26,68 and AisAdapter.hpp position_std_high_accuracy_m=10.0 / position_std_standard_m=30.0

- **[MEDIUM/unverified]** OwnShip RMC emit-side velocity-noise defaults differ from the adapter's assumed RMC noise, so stamped velocity covariance does not match injected noise
  - `sim/OwnShipEmitter.hpp:?` (noise-covariance-mismatch, confidence ?)
  - description: When emit_rmc is enabled, OwnShipEmitter perturbs truth SOG/COG with sigma_sog_emit_m_per_s and sigma_cog_emit_deg (defaults 0.1 m/s, 0.5 deg — OwnShipEmitter.hpp:45-46) and encodes them into $GPRMC. The OwnShipNmeaAdapter builds the pose velocity covariance from its OWN assumed floors sigma_sog_m_per_s / sigma_cog_deg (defaults 0.5 m/s, 1.0 deg — OwnShipNmeaAdapter.hpp:46-47). By default the injected velocity noise is ~5x (SOG) and ~2x (COG) SMALLER than the covariance the adapter stamps, so any velocity-consistency (NEES) evaluation on the own-ship channel is conservatively biased. The two knob sets are documented as intentionally independent, but nothing keeps them equal and the defaults are already inconsistent.
  - evidence: sim/OwnShipEmitter.cpp:53-74 (SOG/COG perturbed by sigma_*_emit); sim/OwnShipEmitter.hpp:45-46 (emit defaults 0.1/0.5); adapters/own_ship/OwnShipNmeaAdapter.hpp:46-47 (assumed defaults 0.5/1.0)

- **[LOW/unverified]** Default report_gps_std=false injects GPS position noise the adapter reports as zero, so target reconstructions inherit unmodeled own-ship error
  - `sim/OwnShipEmitter.cpp:?` (noise-covariance-mismatch, confidence ?)
  - description: OwnShipEmitter adds GPS position noise to own-ship lat/lon whenever gps_pos_std_m>0 (default 5 m; OwnShipEmitter.cpp:37-42), but with report_gps_std=false (the default) it calls adapter_.setPositionStd(0.0) (line 34). ARPA/EO-IR emitters compute range/bearing from the TRUE own-ship position (ArpaEmitter.cpp:87 uses ctx.ownship_truth), while the ArpaAdapter re-projects those detections using the OwnShipProvider's GPS-noisy pose with position_std_m=0 (adapters/arpa/ArpaAdapter.cpp:131). The result is a target-ENU error driven by the injected own-ship GPS noise that no covariance budgets. This is a documented backward-compat default (and the adaptive-UERE test uses it deliberately), but on the default sim path it is an emit-vs-covariance inconsistency.
  - evidence: sim/OwnShipEmitter.cpp:34,37-42; sim/OwnShipEmitter.hpp:20 (report_gps_std default false); adapters/arpa/ArpaAdapter.cpp:131 (sigma_gps_pos = own_opt->position_std_m)

- **[LOW/unverified]** All ARPA clutter false alarms emitted under fixed TTM target number 00 (identical sensor_track_id=0)
  - `sim/ArpaEmitter.cpp:?` (unrealistic-model, confidence ?)
  - description: The ARPA clutter path hard-codes RATTM target number 00 for every false alarm (sim/ArpaEmitter.cpp:70), so all clutter across all rotations arrives with sensor_track_id=0 and collides with any real target left at the default arpa_track_num=0. Real ARPA clutter presents as distinct/unstable target numbers, not one persistent jumping track. This is currently harmless because no associator honors the hint (only core/static/BearingWedgeModel reads sensor_track_id; the GNN/JPDA/MHT comparison tests gate purely on kinematics), but it mismodels the clutter the MHT-vs-JPDA comparison relies on and would collapse all clutter into one track if the ARPA-as-pretracked path were exercised.
  - evidence: sim/ArpaEmitter.cpp:63-77 (clutter body 'RATTM,00,'); adapters/arpa/ArpaAdapter.cpp m.hints.sensor_track_id = target_num; grep shows sensor_track_id consumed only in core/static/BearingWedgeModel

- **[LOW/unverified]** ARPA clutter bearing lacks the 360.000 wire-artifact guard the real-target path has
  - `sim/ArpaEmitter.cpp:?` (edge-case, confidence ?)
  - description: The real-target ARPA path clamps a bearing rounding up to 360 back to 0 (sim/ArpaEmitter.cpp:109-110, b_obs_emit>=359.9995 -> 0.0), but the clutter path formats clutter_bearing_dist_ (uniform [0,360)) with no such guard (lines 68-73). A draw >=359.9995 rounds to the string '360.000', which the ArpaAdapter accepts (it only checks finiteness, not [0,360) range). Cosmetic and self-correcting after wrap, but inconsistent with the guarded real-target path.
  - evidence: sim/ArpaEmitter.cpp:107-110 (guard on real targets) vs 63-73 (clutter, no guard); ArpaAdapter.cpp isFiniteValue(bearing) only

Coverage notes (sim-emitters): Read all sim/ sources and cross-checked each emitter's injected noise against the covariance its consuming adapter stamps. CLEAN: (1) NMEA encoding — wrapWithChecksum XOR-over-body, formatDdmm minute-carry, hemisphere letters, 2-vs-3 degree-digit width all standard-compliant; hand-verified two full GGA sentences in python3 (positive N/E and negative S/W) and confirmed test_nmea_encode round-trips through the real parseNmea and covers minute->degree carry. (2) TruthTrajectory physics — ConstantVelocity integrates p0+v*dt; ManeuveringTrajectory 3-leg (straight/constant-turn/straight) has correct closed-form turn integral and is continuous in BOTH position and velocity at both leg boundaries (verified v_turn(tau2=0)=v0, p continuous, math-convention theta consistent, left-turn sign matches doc). (3) SkewInjector — provably reorders only, never mutates Measurement.time (out.push_back(input[idx]) with original time); deterministic integer 2049-tick jitter ladder (portable, not uniform_real); stable_sort with ingestion-index tie-break; SkewProfile array size 9 exactly matches SensorKind's 9 enumerators (core/types/Ids.hpp:20-22), and at() is bounds-checked. (4) SimulatedSensorBus — per-emitter seeds derived deterministically from cfg.seed via seed_seq; integer step counting avoids dropping the final tick; run() applies a final stable_sort by time; fixed dispatch order (ownship->ais->arpa->eoir). (5) EoIr channel — covariance is consistent by construction because the emitter writes bearing_std_deg/range_std_m directly into the CameraDetection the adapter uses; BearingOnly mode feeds truth range with a wide bearing_only_range_std_m (conservative, not a mismatch); zero-std singularity guards preserve truth-on-wire. (6) ARPA range/bearing std defaults (50 m / 1.0 deg) match between ArpaEmitter and ArpaAdapter. (7) Seeds are fixed in every test (seed=201+k pattern, explicit /*seed=*/N); no time()/random_device nondeterminism found. NOTE on stepOnce: its 'no per-step sort needed' comment is slightly overstated (AIS dropout catch-up could interleave times across emitters within one step) but its only consumers (bias-estimator sweeps in BusComparisonHelpers) do not configure dropout, so it is not currently violated — not reported as a finding.

### unit: baselines (4 findings)

- **[MEDIUM/unverified]** No baseline CSV records toolchain/commit provenance — every run emits git_sha/compiler/host = unknown
  - `docs/baselines/2026-06-07_baseline.csv:?` (provenance, confidence ?)
  - description: All 102 standard baseline CSVs in docs/baselines/ carry `# git_sha: unknown`, `# compiler: unknown`, `# host: unknown` — including the frozen paper-comparison run gospa20m_20260613T174620Z.csv (the source of the Helgesen 43.4/20.37 comparison) and the 2026-07-10 promotion dossier. Backlog #24 documents a harbor config that flips green on g++13 vs red on other compilers, so a frozen benchmark number that cannot be attributed to a compiler version is not reproducible or adjudicable. README caveat #6 acknowledges the field is emitted as `unknown` but it remains unfixed at release. Newer .md write-ups partially compensate in prose (cl1 records commit 7f5cd17 + CSV sha256), but the machine-diffable CSVs themselves — which the comparator joins on — carry none, and even the prose does not pin the compiler version.
  - evidence: `for f in *.csv; do grep '^# compiler:' $f; done` -> 102 files, all `# compiler: unknown`; 0 with a real compiler. Confirmed on 2026-06-07_baseline.csv, 2026-07-10_dossier_harbor_imm_cv_ct_pmbm.csv, gospa20m_20260613T174620Z.csv, cl1_rescore_pmbm_20260708.csv.

- **[MEDIUM/unverified]** Perf/latency baseline has no provenance header and no hardware context — unusable as an off-machine regression reference
  - `docs/baselines/2026-07-06_perf_round2_latency.csv:?` (provenance, confidence ?)
  - description: 2026-07-06_perf_round2_latency.csv is the only baseline CSV with no `#` provenance header at all: bespoke schema (wall_full_s, scan_proc_max_ms, max_fits_interval, peak_rss_mb) and zero record of CPU/host/compiler/build flags. Its numbers (raw_density mean 73.6 ms, max 101.7 ms, `yes_1.45x` fits-interval) are the regression reference the perf-round-3 arc (154->92.9s, worst-scan 137.9->76.7ms in project memory) is measured against, but wall-clock/latency figures with no hardware provenance cannot be reproduced or used as a tripwire on any other machine. The companion frontier CSV notes only `steady-state, warm machine`, still without CPU/host. No committed gtest pins these latency numbers (checked), so it is a doc/baseline quality issue, not a fragile test.
  - evidence: `head 2026-07-06_perf_round2_latency.csv` shows a data header line (workload,wall_full_s,...) with no `#` lines; loop `for f in *.csv; do grep -q '^#' $f || echo $f; done` returns only this file. `grep '^#' 2026-07-05_pmbm_runtime_frontier.csv` has a workload md5 but no host/compiler.

- **[MEDIUM/unverified]** Baselines README is materially stale — documents 5 configs / 8 metrics; harness actually emits 16+ configs (incl. PMBM canonical) and 30+ metrics
  - `docs/baselines/README.md:?` (doc-drift, confidence ?)
  - description: README.md is the index/spec for docs/baselines/, but its §Configurations lists exactly five configs (ekf_cv_gnn, ekf_cv_jpda, ukf_cv_gnn, ukf_ct_gnn, imm_cv_ct_jpda) and §Metrics states 'Eight columns'. The actual harness emits 16 configs in a single run (incl. imm_cv_ct_mht family and the now-canonical imm_cv_ct_pmbm* configs, none of which appear in the config table) and 30+ metric names (gospa_mean/rms/p95/false/missed/localization, card_err_mean, nees_* family, tgospa_raw/smooth, scan_proc_ms_*, wall_seconds). The README is even internally inconsistent: its own AutoFerry headline table cites ekf_cv_mht / imm_cv_ct_mht, configs its Configurations table omits. A new evaluator using README.md as the map is misled about which configs/metrics exist and where PMBM (the current canonical, and the subject of every 2026-07 dossier) fits.
  - evidence: README.md §Configurations table = 5 rows, no MHT/PMBM; §Metrics = 'Eight columns'. `awk -F, '{print $2}' gospa20m_20260613T174620Z.csv | sort -u | wc -l` = 16 configs; distinct non-per-truth metrics in 2026-07-10_dossier_imazu_imm_cv_ct_mht.csv = 31 (card_err_mean, nees_mean, tgospa_raw, scan_proc_ms_max, wall_seconds, ...).

- **[LOW/unverified]** Harbor 'baseline' test is a print-only harness — no guard on the canonical PMBM harbor over-count it claims to baseline
  - `tests/benchmark/test_harbor_complete_truth_baseline.cpp:?` (weak-guard, confidence ?)
  - description: The test is titled TodaysPmbmMetrics and its header calls itself 'the before number the honest static-occupancy layer must improve', but it only prints card_err/gospa/lifetime and asserts EXPECT_GT(lifetime_ratio, 0.3). The reconcile doc establishes card_err_mean=11.64 as the real canonical harbor over-count baseline, yet nothing pins or bands it: a regression that inflated harbor card_err/gospa_false would pass this test unnoticed. Only the non-KEEP adapt_k3 config gets a band guard (test_adapt_k3_harbor_knife_guard.cpp, which is well-designed). The comment is honest that it is 'light sanity asserts only', so this is hygiene, not a false claim — but the file name/comment overstate it as a baseline guard.
  - evidence: test_harbor_complete_truth_baseline.cpp lines 39-62: prints 5 metrics, only assertion is line 61 EXPECT_GT(meanMetric(rows,'lifetime_ratio'),0.3). Reconcile doc 2026-07-10_harbor_truthsort_reconcile.md pins card_err=11.64 as the real number with no corresponding test band.

Coverage notes (baselines): Scope covered: docs/baselines/ (all), tests/benchmark/test_harbor_complete_truth_baseline.cpp, tests/benchmark/test_adapt_k3_harbor_knife_guard.cpp, plus a grep of tests/ for pinned absolute numbers (excluding .venv/site-packages under tests/fixtures/sim_multisensor).\n\nVERIFIED CLEAN:\n- The two target tests use robust bands, not fragile pinned points. adapt_k3 knife guard uses card_err band [8,14] and gospa_false band [1600,2900] that explicitly bracket the lambda_birth-cliff fp-tie (the #24 class of toolchain-fragile flip) with wide margin; both tests ASSERT_NE on the config/scenario so a rename would fail loudly rather than silently skip. This is the correct pattern for the #24 hazard — no fragile pinned assertion found in scope.\n- No committed gtest pins wall-clock/latency/RSS thresholds (grep of tests/ for ms/wall/elapsed/seconds/rss assertions returned only Timestamp/birth_time equality checks and covariance/velocity edge-unit checks, all legitimate).\n- cl1 rescore CSVs (mht/pmbm/ekf) reproduce their frozen sha256 hashes byte-for-byte (all 3 match helgesen2022_reference.md). Per-scenario gospa_rms in cl1_rescore_pmbm match the doc table (18.42/20.83/14.38/20.43/18.35) and the RMS-of-per-scenario proxy recomputes to the headline 18.62 (env-1) and 17.74 (env-2), and 3.43 for the anchored calibration row.\n- Harbor truth-sort reconcile numbers (card_err mht 8.83 / pmbm 11.1 / coverage_land 8.0) match the 2026-07-10 dossier CSVs exactly; the promotion-dossier stale-caveat correction note is present (dated [correction 2026-07-10]).\n- imazu22 doc (2026-07-08) MHT and PMBM per-case numbers reproduce the 2026-07-10 dossier CSVs exactly (imazu_05 mht gospa 24.0/ospa 63.5/card_err 0.10; pmbm 26.3/162.9/0.87; imazu_15 pmbm 30.6/246.8/-0.01/lifetime 0.691) — runs are deterministic/stable across the two dates.\n- Frozen Helgesen table matches gospa20m_20260613 CSV (sc2 40.86->40.9, sc3 46.40->46.4); the stale 43.4 env-1 row is explicitly flagged stale by the doc's own re-score section. Local paper PDF exists at docs/references/S0029801822005753-...pdf.\n- Replay fixtures referenced by the baselines exist: tests/fixtures/philos/out/ais_ferry_near/ (ais/ownship/radar_plots/radar_truth.csv), tests/fixtures/haxr_cfar/out/kattwyk_08_t40.csv, data/dlr/ (kattwyk/parkhafen *-UTC.csv). The many 'missing csv' hits from a path-naive grep were all fixture basenames (ais.csv, ownship.csv, kattwyk_08-UTC.csv) or throwaway debug artifacts (.states.csv/.pmbmbern.csv) not intended to be committed — not real gaps.\n\nNOT DEEPLY VERIFIED: veto_isolation and b25 phase2a/2b docs (reference uncommitted debug artifacts, out of the pinned-baseline scope); exact live value of the adapt_k3 knife-guard band center (~10.74) could not be recomputed without building the bench, but the band is wide and the design is sound.


---

# Reconciliation disposition — 2026-07-15

- **A2 philos** CC-BY-NC-SA committed derivatives — **OPEN: the last release blocker.** DEFERRED by user decision (2026-07-12); the purge-vs-commercial-license disposition is not yet taken. NOT fixed.
- **pohang** CC-BY-NC-4.0 (683 MB, 0 C++ tests) — REMOVED by user 2026-07-12 (on-disk staged data, not git-tracked; matches the MOANA/GFW precedent).
- **Elsevier** publisher-copyrighted PDF (`docs/references/S0029801822005753-helgesen-2022-…`) — still tracked on master; removal **rides fix-wave 6** (hygiene ticket §F).
- **HAXR** truth zero-velocity + sparse-AIS-on-1-Hz-clock OSPA inflation — DEFERRED to **wave 6 W6.2** (derive truth velocity from consecutive AIS; resample truth to the eval clock; A/B and re-record).
- **SMD** no recorded license/provenance; **baselines** lack git-sha/compiler/host provenance; ~2.3 GB fetched-but-unused staged data — **wave-6 hygiene** (§E/§F).
- Verified-clean (no action): circularity check clean on the default paths; autoferry fully validated (CC-BY-4.0); R-BAD's dangerous "reference-tracker-as-accuracy-truth" assumption correctly avoided.
