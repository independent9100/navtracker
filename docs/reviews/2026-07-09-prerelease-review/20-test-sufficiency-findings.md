# Findings — 20-test-sufficiency (appended per batch; raw JSON in 20-test-sufficiency-raw/)

## Batch T1-estimation-assoc-pipeline-geo

### unit: estimation (4 findings)

- **[HIGH/UNCERTAIN]** 2x2 fast-path kernels never exercised with a correlated (off-diagonal) S
  - `core/estimation/GaussianScore.hpp:88` (, confidence ?)
  - description: mahalanobis2x2/gaussianScore2x2 are used exclusively in ImmEstimator::gate() and ImmEstimator::logLikelihood() on the Position2D fast path (ImmEstimator.cpp:503-525, 547-568). Every IMM Position2D test builds S from diagonal inputs only: measurement covariance is always Eigen::Matrix2d::Identity()*var and mode covariances are Identity(5,5) (tests/estimation/test_imm_estimator.cpp AnyModeGateAcceptsNearAnyMode, LogLikelihoodIsModeWeightedMixture..., UpdateShrinksPositionCovariance). With a diagonal S the closed-form cross term (S(0,1)+S(1,0))*y0*y1 is identically zero, so a sign error or index swap in that term is invisible. Grep confirms no test references gaussianScore2x2/mahalanobis2x2/gaussianScore/luInverseDet anywhere, and there is no test asserting the 2x2 kernel equals the generic LU path.
  - verifier: The claim's central factual premise — that the 2x2 fast-path kernels are "never exercised with a correlated (off-diagonal) S" — is FALSE, but its deeper concern (that a cross-term sign error would go uncaught) is only partially refuted, so I land on UNCERTAIN.  WHERE I SEARCHED - grep for gaussianScore2x2/mahalanobis2x2/gaussianScore/luInverseDet/GaussianScore across tests/: only hit is tests/association/test_gating.cpp using an unrelated free function mahalanobisDistance. No test names the 2x2 kernels; no kernel-vs-generic-LU equivalence unit test exists. This half of the claim is TRUE. - The direct IMM unit tests (tests/estimation/test_imm_estimator.cpp) do build S from diagonal inputs only: positionMeas() sets m.covariance = Matrix2d::Identity()*var (line 24) and the hand-built mode covariances are Identity(5,5) (lines 256, 310). So AnyModeGateAcceptsNearAnyMode / LogLikelihoodIsModeWeightedMixture / UpdateShrinksPositionCovariance all feed a diagonal S — the cross term (S01+S10)*y0*y1 is identically zero there. This part of the claim is also TRUE.  WHY "NEVER EXERCISED WITH CORRELATED S" IS FALSE - gate()/logLikelihood() take the fast2d path whenever z.model == Position2D (ImmEstimator.cpp:503-525, 547-568), independent of which motion modes are configured. - ARPA and EO-IR adapters emit Position2D (adapters/arpa/ArpaAdapter.cpp:81,144; adapters/eoir/EoIrAdapter.cpp:67) built via makeMeasurementFromRelativeBearing -> projectRangeBearingToEnu (core/types/MeasurementBuilders.cpp:71-81), which produces a rotated range/bearing covariance whose off-diagonal is nonzero for any non-cardinal bearing. So R itself is correlated, before P even contributes. - These measurements flow through ImmEstimator::gate/logLikelihood in asserted scenario tests: GnnAssociator calls estimator->gate() and -logLikelihood() (core/association/GnnAssociator.cpp:38-41), and tests/sim/test_bus_imm3_comparison.cpp runs an ImmEstimator through GNN on the runBusManeuvering scenario (AIS+ARPA+EO-IR, correlated ARPA/EO R) with an assertion (line 71). PMBM/MHT also call estimator_.logLikelihood/gate on Position2D radar (core/pmbm/PmbmTracker.cpp:290,467,786,792; tests/benchmark/test_imazu22_scenario_run.cpp uses config imm_cv_ct_mht with a firm id_switches bound at line 161). So the 2x2 kernel IS exercised with a genuinely correlated S under assertions — the auditor missed this.  WHY IT IS ONLY UNCERTAIN, NOT FULLY REFUTED I cannot point to one assertion and prove it flips on a cross-term sign error: - The most direct IMM scenario tests (tests/scenario/test_filter_comparison.cpp ManeuveringTarget, Maneuvering3ModeIMM) run IMM through GNN but place NO EXPECT on the IMM output — they only fprintf its metrics. They would not catch anything. - test_bus_imm3_comparison.cpp asserts only the soft comparative "IMM beats CV on OSPA OR id_switches" over a single-target scenario, where association is trivial; a mis-scored d2 near a well-tracked lone target is unlikely to reject the true measurement, so a sign flip may not move the aggregate past the assertion. - The strong multi-target correlated-S firm-assertion tests (imazu imm_cv_ct_mht id_switch bound; harbor/philos/haxr PMBM OSPA replays) are exactly where a sign flip could plausibly break an assertion, but the bounds carry margin and these are fixture-gated, so I cannot confirm the breakage flips them.  Net: the literal "never exercised with correlated S" is refuted by the ARPA/EO/PMBM path; the residual "no test reliably catches a subtle cross-term sign error" has merit but is not established. Coverage exists (exercise under assertions) yet its catching power for this specific breakage is questionable — UNCERTAIN.

- **[MEDIUM/unverified]** IMM softUpdate normalized-likelihood branch (V_j gate volume) is never covered
  - `core/estimation/ImmEstimator.cpp:395` (, confidence ?)
  - description: The ctx.p_d>0 && ctx.gate_threshold>0 branch computes the per-mode gate volume V_j = c_d*gamma^d*sqrt(det) with c_d = 2 for d==1 and pi for d==2 (lines 395-405). This path is production-active: JpdaAssociator sets out.p_d (JpdaAssociator.cpp:76/81) and Tracker::softUpdate forwards ctx (Tracker.cpp:250). But every ImmEstimator::softUpdate unit test calls it with a default-constructed PdaContext (p_d=0 -> proxy branch): tests/estimation/test_imm_estimator.cpp SoftUpdateWithSingleConfidentMeasurement... and SoftUpdateActuallyMovesState... pass only 4 args. And tests/scenario/test_multi_seed_sweep.cpp runs JPDA (runBatched/softUpdate) only with EkfEstimator; the IMM runs use gnn/runStandard (hard update). So no unit or scenario test drives IMM+JPDA with p_d>0.

- **[MEDIUM/unverified]** ParticleFilterEstimator::update lacks the R-validity guard that EKF/UKF/IMM all have
  - `core/estimation/ParticleFilterEstimator.cpp:79` (, confidence ?)
  - description: EKF, UKF, and IMM each call isMeasurementCovariancePsd(z.covariance) at update entry and skip on failure (EkfEstimator.cpp:37, UkfEstimator.cpp:61, ImmEstimator.cpp:170 — the documented Phase 8 R3 fix against 'one bad NMEA frame poisoning a track'). ParticleFilterEstimator::update has no such guard and calls z.covariance.inverse() directly (line 79). PF::initiate guards via LLT, and PF::update early-returns when particles.cols()==0, so a bad-R birth is safe — but a track born with good R that later receives a measurement carrying the documented empty 0x0 'no uncertainty' sentinel or a non-PSD R reaches inverse() on an empty/degenerate matrix. tests/estimation/test_particle_filter_estimator.cpp exercises only good-R updates; PredictOnEmptyEnsembleIsNoOp covers the empty-ensemble path, not a bad-R update on a populated ensemble.

- **[MEDIUM/unverified]** No end-to-end NEES/NIS consistency test on a real estimator run
  - `tests/benchmark/test_consistency.cpp:153` (, confidence ?)
  - description: The Consistency machinery (NisCollector, computeNees) is thoroughly validated against synthetic IID Gaussian draws and hand-crafted BenchResults (tests/benchmark/test_consistency.cpp: NisFromIidGaussianResiduals, NeesFromKnownFilter, NisFittedAlpha, SingularSDropped). But no test runs an actual EKF/UKF/IMM/PF over a synthetic truth+noise scenario and asserts the estimator's own reported covariance is statistically calibrated (NEES/NIS inside the chi-square band). Scenario tests assert accuracy/identity only: tests/scenario/test_filter_comparison.cpp and test_multi_seed_sweep.cpp check mean_ospa, final_track_count, id_switches — never covariance magnitude vs empirical error. Estimator unit tests assert covariance only qualitatively (EXPECT_LT/GT shrink/grow).

Coverage notes (estimation): WELL COVERED: (1) Measurement Jacobians — RangeBearing2D is checked against finite differences (tests/estimation/test_measurement_models.cpp JacobianMatchesFiniteDifference) plus analytic pins; Bearing2D is pinned analytically (Bearing2DPredictAndJacobian) and its single row equals the finite-diff-checked RangeBearing bearing row; offset-sensor variants and state-dimension adaptation covered. (2) SigmaPoints robustness fully covered: weights-sum-to-one, mean & covariance reconstruction, non-PD rank-1 fallback, and indefinite/negative-eigenvalue clamping all stay finite (test_sigma_points.cpp). (3) CoordinatedTurn omega=0 CV-limit branch AND finite-omega rotation both asserted, plus propagate reads omega from state, processNoise diagonal, and UKF>EKF on an arc (test_coordinated_turn.cpp, test_ukf_estimator.cpp BeatsEkfOnCoordinatedTurnQuarterArc). (4) IMM dt-scaled TPM: dt=1 short-circuit, pi^dt scaling, and the semigroup/composition property (ModeTransitionIsDtScaled, ModeTransitionComposesAcrossPredicts). (5) IMM any-mode gate and mode-weighted-mixture logLikelihood vs moment-matched baseline (but only with diagonal S — see finding 1). (6) UKF==EKF agreement on linear predict and linear update; UKF/EKF inner-filter equivalence inside IMM for linear h. (7) PSD/NaN guards on EKF/UKF/IMM update and initiate, and on IEstimator::gate/logLikelihood defaults (fails-closed / returns -inf); empty 0x0 and non-PSD covariance both tested for EKF initiate. (8) StudentTNoiseModel scale formula (inlier/outlier/clamp) plus end-to-end EKF outlier resistance; GaussianNoiseModel always-1. (9) PF determinism-by-seed, ESS-triggered systematic resampling, CT per-particle propagate, 5-state sizing, empty-ensemble no-op. (10) NIS/NEES collector internals validated with IID draws incl. Wilson-Hilferty band, fitted alpha/beta, singular-S drop, low-sample suppression. dt<=0 predict guard exists in all estimators (EKF/UKF/PF/IMM) and is defensively correct but has no direct no-op test on a populated track — judged low risk (out-of-order dt is prevented upstream by the reorder buffer) so not filed as a finding. COULD NOT DETERMINE: whether the fixture-gated replay tests (replay/test_philos_*, test_haxr_*) drive IMM with correlated-S Position2D or IMM+JPDA; they assert end-to-end OSPA only, so even if they run they would not pin the 2x2 cross-term or the softUpdate V_j normalization, and they SKIP without fixtures. No direct absolute-value check exists on the generic gaussianScore/luInverseDet output, but it is exercised indirectly through IMM/IEstimator logLikelihood and association behavior tests.</coverage_notes> </invoke>

### unit: association-pmbm (5 findings)

- **[HIGH/CONFIRMED]** Same-scan two-sensor fusion of ONE target is untested end-to-end
  - `core/pmbm/PmbmTracker.cpp:760` (, confidence ?)
  - description: The point-target PMBM update builds a single (n+m)x m cost matrix and assigns at most ONE measurement per Bernoulli per scan (cost matrix + Murty at PmbmTracker.cpp:760-840). So when two sensors (e.g. radar + AIS/cooperative) report the SAME vessel in one processBatch, one return updates the Bernoulli and the other births a competing new-target Bernoulli; the design relies on mergeBernoulliDuplicates (PmbmTracker.cpp:1930, default bhattacharyya_merge_threshold=1.0) to collapse them into one track. Every multi-sensor fusion scenario test DELIBERATELY avoids this: measurements are staggered into separate scans with explicit comments 'no two-in-one-scan competition' (tests/pmbm/test_pmbm_remote_track_fusion.cpp:192 and :321; tests/pmbm/test_pmbm_sensor_activity.cpp:1133 FuseOneTrackStableThroughCoopDropout). tests/pmbm/test_pmbm_phase8.cpp:344 ('two simultaneous measurements') uses two DIFFERENT targets, not one target seen by two sensors. tests/pmbm/test_pmbm_clutter_feed.cpp:130-131 and :162-171 explicitly state the collapse case 'cannot be isolated in a unit test'. mergeBernoulliDuplicates is unit-tested only on hand-constructed Bernoullis, never as the outcome of a two-in-one-scan processBatch.
  - verifier: I confirm the auditor's claim: no test exercises same-scan two-sensor fusion of ONE target end-to-end through processBatch, and none would catch a duplicate-track / ID-churn regression from the two competing same-scan births failing to merge.  Source verified (core/pmbm/PmbmTracker.cpp:760-840): the point-target update builds one (n+m)×m cost matrix and runs Murty, assigning at most one measurement per Bernoulli per scan; a second same-target measurement in the same batch lands on a new-target diagonal row and births a competing Bernoulli. Collapse is left to mergeBernoulliDuplicates (Bhattacharyya threshold).  The design intent and the untested residual are stated explicitly in tests/pmbm/test_pmbm_clutter_feed.cpp:162-178: an earlier guard against folding two same-scan distinct-claim Bernoullis was REVERTED precisely because "a fused batch is multi-sensor, so one physical target detected by two sensors ... legitimately yields two distinct-claim Bernoullis that MUST merge; the guard would leave a duplicate track per vessel." So the merge is load-bearing, yet it is never verified as the outcome of a two-in-one-scan batch.  Coverage I searched and ruled out: - Multi-sensor fusion scenario tests deliberately stagger measurements into separate scans: tests/pmbm/test_pmbm_remote_track_fusion.cpp:191-192 ("Staggered so no two-in-one-scan competition") and :320-321, with confirmedCount==1 assertions (:230,:246,:255,:348) that therefore never hit the same-scan path; tests/pmbm/test_pmbm_sensor_activity.cpp:1132-1133 (FuseOneTrack..., "no two-in-one-scan competition; they gate to the same Bernoulli"). - tests/pmbm/test_pmbm_clutter_feed.cpp:130-133 TwoTargetsBothReturnsClaimed uses TWO DIFFERENT targets 25 m apart and explicitly says "the collapse case cannot be isolated in a unit test." - Bhattacharyya merge is unit-tested only on hand-constructed Bernoullis seeded via mutableDensityForTesting() with a far-away single measurement to trigger the fold: tests/pmbm/test_pmbm_phase8.cpp:173 (BhattacharyyaMergeKeepsOlderIdAndDeletesYounger, near-coincident 0.0/0.1) and :237 (merge-off). These never run a two-sensor processBatch, and cover only the near-coincident (merge-succeeds) and merge-disabled cases — not the sensor-bias-offset case where distance exceeds threshold and the duplicate persists. - The phase8 same-scan multi-measurement batches ({z_hi,z_lo} at :369,:500,:540 and the T4 per-sensor model test at :344) all use TWO DIFFERENT targets (e.g. 0,0 vs 100,100) and assert bernoullis.size()==2 — the opposite of one-target-two-sensors. - PDA tests (tests/pmbm/test_pmbm_pda_soft_update.cpp:70,:130) feed two same-scan measurements but they are one established track + clutter (clutter-pull resistance), default-off PDA branch, no single-track-out assertion for two-sensor same-target. - The canonical integration example tests/integration/test_full_stack_pipeline.cpp uses the GNN/MHT Tracker with a single AIS stream, not the PMBM same-scan two-sensor path.  No PMBM scenario/replay test feeds two sensors reporting the same vessel at the same timestamp and asserts exactly one output track, so the specific silent regression (two same-scan births offset by a sensor bias exceeding the Bhattacharyya merge threshold → persistent duplicate or ID churn per multi-sensor vessel) would go uncaught.

- **[MEDIUM/unverified]** Velocity-runaway innovation gate (#25 Phase 2b) has no behavioral test
  - `core/pmbm/PmbmTracker.cpp:973` (, confidence ?)
  - description: The innov_gate_max_m guard is a three-part mechanism: the true raw position innovation is computed on each detected cell (PmbmTracker.cpp:807-818), the gate fires when it exceeds innov_gate_max_m on the assigned Bernoulli (PmbmTracker.cpp:973-976), and applyInnovationGate clamps/resets velocity+turn-rate and inflates their covariance on the moment-matched state AND every IMM mode (PmbmTracker.cpp:2326-2367, config innov_gate_action / innov_gate_velocity_var_floor). A constrained grep of tests (excluding tests/fixtures) finds these symbols only in tests/benchmark/test_config.cpp (config parsing) — no test ever triggers the gate or checks the clamped state.

- **[MEDIUM/unverified]** JPDA joint-event overflow -> greedy hard-assignment fallback is untested
  - `core/association/JpdaAssociator.cpp:161` (, confidence ?)
  - description: Two coupled overflow paths have no coverage. (1) enumerateJointEvents(V, max_events) returns {} on overflow (core/association/JointEvents.cpp:24,52) but tests/association/test_joint_events.cpp only calls the unbounded overload — the max_events cap and empty-on-overflow contract are never tested. (2) When enumeration overflows, JpdaAssociator degrades the whole gating cluster to a greedy mutual-exclusion hard assignment (betas -> 0/1) and sets overflow_fallback=true (JpdaAssociator.cpp:161-185); a constrained grep finds overflow_fallback asserted in no non-fixture test. Also the degenerate normalization fallback (sum<=0 or non-finite -> all-miss, JpdaAssociator.cpp:216-220) is untested.

- **[LOW/unverified]** JPDA estimator-routed betas (production path) are executed but never asserted
  - `core/association/JpdaAssociator.cpp:99` (, confidence ?)
  - description: Production wires JPDA through Tracker.cpp:142/221 as associate(tracks, batch, &estimator_), taking the estimator branch (JpdaAssociator.cpp:99-101: estimator->gate + exp(estimator->logLikelihood)). Every JpdaAssociator unit test in tests/association/test_jpda_associator.cpp calls associate WITHOUT an estimator, exercising only the inline single-Gaussian density path (JpdaAssociator.cpp:103-116). The scenario tests that do route through an estimator (tests/scenario/test_jpda_comparison.cpp) print OSPA/id_switch diagnostics to stderr with no EXPECT/ASSERT.

- **[LOW/unverified]** PPP component cap (max_ppp_components) keep-top-weighted behavior untested
  - `core/pmbm/PmbmTracker.cpp:1284` (, confidence ?)
  - description: pruneAndNormalise caps the PPP at max_ppp_components (default 200) via partial_sort keeping the highest-weight components (PmbmTracker.cpp:1284-1293). max_ppp_components appears in no non-fixture test; no test drives the PPP past the cap to confirm the strongest components survive. The comment notes the cap only matters on long replays under measurement-driven birth, where the PPP grows unbounded otherwise.

Coverage notes (association-pmbm): Well covered: (1) Murty K-best — uniqueness on square 3x3, non-decreasing costs on 5x5, full brute-force enumeration of all 6 permutations of a 3x3, forbidden-cell honoring, stop-at-feasible-count, rectangular surplus-cols first-assignment + monotonicity, and K<=0/empty edges (tests/association/test_murty.cpp). I also hand-verified the Miller-Stone-Cox partition is exhaustive and non-duplicating even for the (n+m)x m surplus-ROW shape PMBM actually feeds, so the thin direct rectangular test is backed by correctness of the algorithm plus heavy PMBM integration (MurtyPicksMatchedAssignmentAsBestChild, MixtureWeightsSumToOneAfterUpdate). (2) Hungarian — square min-sum, rectangular more-cols-than-rows, forbidden cells, and the degenerate all-inf-row no-hang pathology (tests/association/test_hungarian.cpp); the more-ROWS-than-cols surplus path (rows->-1) is only exercised transitively via Murty/PMBM, which is adequate. (3) JPDA — soft betas, ungated->0 beta, column+beta_0=1 normalization incl no-detection event, empty inputs, and per-sensor mode (single-sensor==scalar, per-measurement lambda_C isolation, out-of-coverage no-miss, homogeneous vs heterogeneous batch p_d) (tests/association/test_jpda_associator.cpp). (4) JointEvents — no-measurement single-empty-event, gating restriction, mutual-exclusion feasibility count (tests/association/test_joint_events.cpp). (5) PMBM — extensive: predict decay/propagation/zero-dt, misdetection existence math, PPP birth/decay-by-(1-Pd), r_min and hypothesis-weight-min pruning, MBM hypothesis cap, log-sum-exp normalization + all-(-inf) uniform keep-alive (exercised via pruneAndNormalise), Bhattacharyya merge on/off (older-id survivor), adaptive birth r_new=lambda ratio + out-of-range fallback, per-sensor Pd/lambda_C differentiation, PDA soft-detected branch, sensor-activity miss model + hypothesis-order consistency, source-aware misdetection/identity gate, clutter-feed claim credit, per-track view rebuild idempotence/match-flat, RTS smoothing, TPMBM trajectory window, replay determinism, and lifecycle events. Recycling is NOT a concern — no recycle-to-PPP path exists in the source (attention item does not apply). Could not fully determine: whether any long-replay fixture test drives the PPP past max_ppp_components or ever trips the JPDA 1e6-event cap (fixtures present but not built/run here) — but overflow_fallback is asserted in zero source-level tests regardless. Not built/executed anything (read-only tree).

### unit: pipeline-tracking (3 findings)

- **[MEDIUM/unverified]** ID-never-reused invariant is not asserted across any delete->create cycle
  - `core/tracking/TrackManager.cpp:12` (, confidence ?)
  - description: CLAUDE.md invariant #5 and both class docs promise stable TrackIds that are never reused after deletion. The only tests that touch id allocation (tests/tracking/test_track_manager.cpp::AssignsMonotonicStableIds asserting id1=1,id2=2; ResolvesIdsCorrectlyAfterMiddleErase which only checks post-erase lookups; tests/pipeline/test_mht_cross_merge.cpp asserting the surviving id stays 1u) never delete a track and then add a NEW one to check the fresh id is strictly greater than every id ever issued and never equals a deleted id. I grepped tests/ for 'reuse/never reuse/next_id' (only a PMBM comment match), and every mgr.add(...) call site in tests/tracking and tests/pipeline. The invariant currently holds only because both allocators are bare monotonic counters (next_id_++ / next_external_id_++). A regression that recycled a freed id/slot (e.g. deriving the id from tracks_.size(), or resetting the counter when the set empties) would pass the entire suite.

- **[LOW/unverified]** SPRT confirm-threshold tests use sprt_alpha == sprt_beta, so an alpha/beta swap or asymmetry regression is invisible
  - `core/pipeline/MhtTracker.cpp:698` (, confidence ?)
  - description: t_confirm = log((1 - sprt_beta)/max(sprt_alpha,1e-12)). The only SPRT tests (tests/pipeline/test_mht_sprt_confirm.cpp) set sprt_alpha = sprt_beta = 0.01, which makes T_confirm = ln(0.99/0.01) symmetric in the two parameters. CleanTargetConfirmsUnderSprt and SingleDetectionDoesNotConfirm would both still pass if the formula accidentally swapped alpha and beta, dropped the (1-beta) numerator, or otherwise became asymmetric, because the numeric threshold is unchanged when the two inputs are equal. No test varies alpha vs beta to pin the direction/magnitude of the threshold.

- **[LOW/unverified]** The M-of-N confirmation branch in MhtTracker is never asserted to promote a track
  - `core/pipeline/MhtTracker.cpp:703` (, confidence ?)
  - description: The confirm gate falls through to M-of-N (countHitsInWindow(leaf, confirm_hits_window) >= confirm_hits_needed) only when use_ipda_lifecycle=false AND use_sprt_confirm=false. The default config has IPDA on, and the SPRT tests set sprt=true. The one test that reaches the pure M-of-N path (tests/pipeline/test_mht_tracker.cpp::ObserveBundleFullWeightWhenNoTreeClaims, ipda=false/visibility=false) asserts nothing about track status. So the confirm_hits_needed/confirm_hits_window wiring at the MhtTracker level (the >= comparison and which config fields feed it) has no assertion; only the underlying TrackTree::countHitsInWindow primitive is unit-tested (tests/tracking/test_track_tree.cpp:185-189).

Coverage notes (pipeline-tracking): WELL COVERED (good news for synthesis): (1) Stale-input guard for both single-hyp Tracker and MhtTracker, including the equal-timestamp boundary (Tracker::EqualTimestampIsNotStale, MhtTracker::EqualTimestampScanIsNotStale), opt-out, and byte-identical parity vs a clean twin (test_tracker.cpp:214-284, test_mht_tracker.cpp:98-145). (2) ReorderBuffer drop policy is unusually thorough: unit tests plus tests/scenario/test_reorder_buffer_e2e.cpp verifies determinism under skew, monotonic drains, comfortable-window-drops-zero, and tight-window drop count matching an independently-computed ground-truth late count over crossing + AIS-dropout scenarios. Multimap equal-key ordering is deterministic (C++11) and exercised indirectly by the determinism tests. (3) MeasurementBuilders covariance composition is checked against hand-computed projectRangeBearingToEnu references, including the #16 heading-sigma quadrature composition, the config-floor clamp (floor only widens), per-pose-sigma widening, the zero-covariance sentinel + applyDefaultsIfEmpty path, no-datum/no-pose empty returns, and pose-at-timestamp selection. (4) batch-ordering: MhtTracker and PmbmTracker sort unsorted batches to match sorted input and are determinism-checked (test_batch_ordering.cpp). (5) TrackTree::pruneNScan depth semantics have two dedicated tests (KeepsBestTrunk, PreservesProtectedAncestorChain) plus MhtTracker smoke tests for n_scan_extension_when_protected and score_delta_threshold not exploding trees. (6) TrackManager lifecycle (confirm-on-consecutive-hits, coast-then-delete, middle-erase reindex, delete-fires-before-erase, null-sink safety) and the cross-tree duplicate-merge ID-stability (older id survives) are covered.  COULD NOT FULLY DETERMINE / notes: (a) TrackManager sink REENTRANCY (a sink calling back into add/recordMiss during onTrackDeleted, which fires before the erase+reindex at TrackManager.cpp:50-62) is neither tested nor documented as supported; if a consumer does it, the mid-erase vector/index mutation is a latent hazard, but it is not clearly an intended behavior so I did not raise it as a finding. (b) Asymmetry vs the base Tracker: MhtTracker/PmbmTracker sort batches internally but Tracker::processBatch does NOT (it uses scan.front().time as the single scan instant); this is consistent with its documented same-timestamp-batch precondition, so it is a precondition rather than a bug. (c) The pipeline's initiate-guard branches (canInitiateTrack rejecting lone Bearing2D, isMeasurementCovariancePsd rejecting non-PSD covariance before seeding) have only indirect/fixture-gated coverage (tests/replay/test_camera_bearing_loader.cpp) plus estimation-layer unit tests; a direct single-hyp pipeline assertion that a lone bearing / NaN-covariance measurement seeds no track is absent, but adapters validate at the edges (invariant #6) so I rated it below the bar. Fixture-gated replay tests may SKIP if data/ is absent.

### unit: geo-ownship-bias (4 findings)

- **[MEDIUM/unverified]** datumAxisRotation does not wrap longitude delta — antimeridian recenter rotates track velocity/covariance by a bogus ~360 deg
  - `core/geo/AxisRotation.cpp:15` (, confidence ?)
  - description: datumAxisRotation computes delta_lon_rad = (n.lon_deg - o.lon_deg) * kDeg2Rad with no normalization to [-180,180]. When an auto-recenter fires with the old and new datum origins straddling the +/-180 meridian (e.g. old=+179.9, new=-179.8), the true convergence-angle delta of ~0.3 deg is computed as ~-359.7 deg. The resulting R rotates every track's velocity and position/covariance blocks (via shiftTracksOnDatumChange -> rotateVelocityInPlace/rotateCovarianceInPlace in core/tracking/DatumShift.cpp) by that bogus angle. Note the POSITION shift itself is antimeridian-safe (it goes through ECEF in Datum::toGeodetic/toEnu), so positions stay correct while COG and the error-ellipse orientation are silently corrupted — a hard-to-notice failure. tests/tracking/test_datum_shift.cpp and tests/scenario/test_datum_recenter_scenario.cpp only exercise 8.0->8.5E / 8.0->9.0E (small positive deltas); no test crosses the antimeridian or uses negative longitudes.

- **[MEDIUM/unverified]** Projected covariance off-diagonal (ellipse orientation) never independently asserted at a non-cardinal bearing
  - `core/projection/Projection.cpp:21` (, confidence ?)
  - description: projectRangeBearingToEnu builds cov = J R J^T with J = [[sb, r*cb],[cb, -r*sb]]. Every direct test in tests/adapters/util/test_projection.cpp uses bearing = pi/2 or 0, where the analytic off-diagonal cov(0,1) is exactly 0 (and is asserted =0). The only non-cardinal case (GpsStdComposesWithExistingNoise, bearing=1.0) asserts only the DIFFERENCE (with_gps - base) off-diagonal is ~0, never base's off-diagonal value. tests/types/test_measurement_builders.cpp (RelativeBearingProducesEnuConsistentWithDirectProjection, rel_bearing=30deg) checks m.covariance(i,j) == projectRangeBearingToEnu(...).cov(i,j) — a tautology comparing the function to itself. So the sign/magnitude of the cross-correlation term for an off-axis bearing is never verified against an independent analytic value.

- **[LOW/unverified]** Bearing-bias outlier gate has no rejection test (only the position gate is covered)
  - `core/bias/SensorBiasEstimator.cpp:154` (, confidence ?)
  - description: observe(BearingBiasPairObservation) has a Mahalanobis outlier gate (r*r > outlier_sigma^2 * S) that protects a converged EOIR bearing bias from a single bad AIS/coop-anchored pair. tests/bias/test_sensor_bias_estimator.cpp tests the POSITION outlier gate (PositionOutlierGateRejects, rejectedByOutlier==1) and bearing CONVERGENCE (BearingConvergesToTrueBias), but no test drives a large bearing residual through the bearing path to confirm it is rejected. rejectedByOutlier() is a shared counter, so even indirectly the bearing branch is unverified.

- **[LOW/unverified]** Two NavInputGuard branches uncovered: derived-SOG low-speed flag and heading-delta wrap
  - `core/own_ship/NavInputGuard.cpp:52` (, confidence ?)
  - description: tests/own_ship/test_nav_input_guard.cpp covers low-SOG via velocity_is_valid=true, stale, position_jump, heading_jump, first-pose, and out-of-order. Two branches are unexercised: (1) the derived-SOG path (line 52-53: velocity_is_valid=false, dt>0, sog = step_m/dt below heading_min_sog_mps) that sets heading_unreliable_low_sog — the one position_jump test with sog_valid=false uses a huge step so this low branch never fires; (2) headingDeltaDeg's wrap arm (line 22-24, d>180 -> 360-d), e.g. heading 359->1 should be 2deg not 358deg. HeadingJumpFlagged only uses 0->90 (no wrap).

Coverage notes (geo-ownship-bias): WELL COVERED (good news for synthesis): (1) Bias sign conventions per observation kind are all sign-sensitive and would go red on a flip: GpsHeading (biasRad ~= K*kBiasTrue), Cog (EXPECT_GT 0), Magnetic (EXPECT_GT 0), AisArpa (SinglePairMovesBiasTowardTruth EXPECT_GT 0), BearingInnovation (biasRad ~= K*r), SensorBias position (true_b(3,-2) both axes) and bearing (0.02), plus the full-pipeline BearingBiasConvergence scenario reconstructs +kBiasTrue. The specific sign-flip concern in the prompt is NOT a gap. (2) HeadingBiasEstimator: all five MHS kinds have accept + gate-rejection tests (low-SOG, turning, null-variation noop, outlier), plus range/state-var/outlier gates on BearingInnovation, wrap-around, drift tracking, GPS floor, and publish/stale gating. (3) SensorBiasEstimator: convergence, per-key independence, position range+outlier gates, seed-anchoring semantics, and the full cross-sensor extractor suite (one-obs-per-key, same-source-id skip, Schmidt fold, deterministic key order, existence/cov/AIS eligibility gates, pinned-anchor recovery). (4) OwnShipProvider: exhaustive — empty/latest, poseAtOrBefore exact/earlier/nullopt, out-of-order insertion, equal-timestamp last-wins, history eviction, datum init/threshold/recenter/policy-off/multi-sink/unregister/explicit-ctor. (5) DatumShift (AxisRotation consumer): asserts the exact convergence angle gamma and velocity/covariance rotation. (6) UERE and OwnShipVelocity: window gating, convergence across sigma range, maneuver suppression + resume, min-sigma floor, velocity output sign. (7) Projection: diagonal covariance at cardinal bearings, heading-in-quadrature, GPS isotropic add. (8) WGS84/Datum round-trips are exact (ECEF-based inverse, so the '30 km round-trip error bound' concern is moot — no small-angle approximation is used; error is machine precision at any range).\n\nNOT FULLY DETERMINED / OUT OF THIS UNIT'S SOURCE SCOPE: The prompt's 'integration-level recenter with occupancy/coastline/static sinks all registered' — those models (core/static, core/land) are outside this unit's source files. The recenter FIRING mechanism (OwnShipProvider) is well tested with CountingSink/TrackShifterSink, and each model's own onDatumRecentered is tested in tests/static/* and tests/land/* individually, but I did not find a single scenario test that wires OwnShipProvider auto-recenter together with TrackManager + occupancy + coastline + static sinks to confirm no cache goes stale in the assembled pipeline; flagging for the synthesis owner of the static/land units. Also not covered anywhere in-scope: southern-hemisphere / negative-longitude datums (all geo/own_ship/datum-shift tests use 53.5N/8E); the math is sign-symmetric so risk is low, but directional (north/east) assertions exist only for the northern hemisphere.


## Batch T2-collision-adapters-scenario

### unit: collision-output-static (5 findings)

- **[MEDIUM/unverified]** Hysteresis band width (enter_probability vs exit_probability) is never isolated by a test
  - `core/collision/CpaEvaluator.cpp:37` (, confidence ?)
  - description: CpaEvaluator's risk state uses two thresholds: a track enters risk at p>=enter_probability but only clears at p<exit_probability (line 36-38), which is the whole point of the enter/exit config pair. The existing unit tests cover Entered (EnteredFiresOnHighRisk), no-refire-while-risky, and Exited-via-deletion (DeletedTrackFiresExited). The full-stack integration test covers a probability-driven Exit, but in that head-on pass p collapses from ~1 straight through the band to ~0, so it never distinguishes exit_probability from enter_probability. No test drives p into the band (exit_probability <= p < enter_probability) after entering and asserts the pair stays Entered (held), nor asserts it does NOT re-enter after dropping just below enter.

- **[MEDIUM/unverified]** StaticHazardEvaluator emit_updates / Updated transition has zero coverage
  - `core/collision/StaticHazardEvaluator.cpp:44` (, confidence ?)
  - description: StaticHazardEvaluatorConfig.emit_updates gates a per-cycle Updated event while own-ship stays inside the keep-clear ring (evaluate() line 44-47). grep across tests/ shows emit_updates and CollisionRiskTransition::Updated are only exercised for CpaEvaluator (test_cpa_evaluator.cpp:157,165); StaticHazardTransition::Updated appears in no test. tests/collision/test_static_hazard_evaluator.cpp only covers Entered/Exited/hysteresis/null-sink/colocated. The config knob is default-off and completely untested.

- **[MEDIUM/unverified]** position_uncertainty_m expansion of the hard-suppression core is untested
  - `core/static/StaticObstacleModel.hpp:61` (, confidence ?)
  - description: birthSuppression computes r_hard = footprint_radius_m + position_uncertainty_m (line 61-62), so a charted obstacle with position uncertainty gets a larger c=1.0 hard core. Every test in test_static_obstacle_model.cpp (and the integration test) uses obstacles with position_uncertainty_m at its default 0, so the '+ position_uncertainty_m' term is never exercised; the ramp/clamp/datum tests would all pass even if that term were dropped.

- **[LOW/unverified]** COG wrap into [0,360) for south/west-bound velocities is untested
  - `core/output/TrackOutput.cpp:40` (, confidence ?)
  - description: toVelocityOutput maps velocity to COG via atan2(v_east,v_north) then wraps negative results with cog_deg += 360 (line 38-41), and the header contracts COG in [0,360). test_track_output.cpp only exercises due-east (COG 90) and due-north (COG 0) velocities — both non-negative before wrap. No test uses a velocity with negative east or negative north (COG in (180,360)) that actually triggers the +=360 branch.

- **[LOW/unverified]** Non-positive interval (clock skew) and multi-surveillance p_D aggregation untested
  - `core/sensor_activity/DeclaredSensorActivity.cpp:39` (, confidence ?)
  - description: evaluate() returns an empty MissOpportunity when dt<=0 (now<=last_checked, line 38-39) and, across multiple Surveillance profiles that both fire, keeps the strongest p_D (line 47-48). test_declared_sensor_activity.cpp covers single-channel surveillance miss/in-range/mid-sweep/sector in-and-out and a single cooperative-overdue, but never passes now<=last_checked, and never configures two surveillance channels to check p_D is the max.

Coverage notes (collision-output-static): Overall this scope is unusually well tested; the findings above are edges, not holes in core behavior.  WELL COVERED (verified by reading the test files): - Cpa/computeCpaWithUncertainty (test_cpa_uncertainty.cpp): zero-covariance->zero-sigma, head-on position-sigma propagation, perpendicular-pass linear sigma scaling, parallel-velocity branch (sigma_tcpa=inf, sigma_cpa from dp cov), past-CPA/diverging branch, head-on near-zero-cpa isotropic fallback, probability-vs-Gaussian sweep, monotonicity in sigma. Zero-relative-velocity CPA is covered here. - CpaEvaluator: no-own-ship no-op, Entered on high risk, no-refire-while-risky, track-deleted-while-Entered -> Exited (DeletedTrackFiresExited), emit_updates gating, Tentative skipped by default. The probability-driven Exit on a receding target IS covered end-to-end by tests/integration/test_full_stack_pipeline.cpp (head-on pass, deleted.size()==0 so the Exit is via probability, not deletion) — only the band-WIDTH distinction (finding 1) is unverified. - synthesizeOwnShipTrack: ENU placement, position cov, valid/invalid velocity cov (test_cpa_synthesize_own_ship.cpp). - TrackOutput: NED covariance NUMERICALLY asserted incl. high-latitude convergence-angle rotation with exact expected R and off-diagonal (CovarianceRotatedAtHighLatitude); SOG/COG for E and N; stationary singularity; velocity_observed gate; zero-vel-cov / 2D-state invalid velocity; covariance_is_default forwarding both ways. - LiveOccupancyModel: extremely thorough — conservation invariant (SuppressionImpliesAnEmittedHazardCoversIt), datum recenter re-anchor (DatumRecenterKeepsGeographicSuppression), negative-coordinate cells (compA/compB at -1012.5 exercise std::floor cellOf), coverage-aware decay (in/out of range and sector), shadow-guard monotonicity on fixed inputs, corroboration veto A/B + anchored window, camera eviction/flicker/self-heal, clutter-adaptive bar, bounded-latency recovery, determinism. - ShadowMask: wrap-through-pi occluder as one cluster (BearingWaparoundIsOneOccluder), weak-cluster no-shadow, wedge width from extent, multiple occluders, range margin, disabled. (Note: ShadowMask uses ENU math bearings so its wrap is at +/-pi = west, not north; wrap is covered.) - StaticObstacleModel: ramp values numerically (inside/just-outside/mid/beyond), soft_max clamp both directions, datum recenter rebuild. - StaticHazardEvaluator: enter/exit hysteresis, null-sink, stays-outside, co-located independent hysteresis; staticHazardId source_id disambiguation. - BearingWedgeModel: claim = per-drain suppression not deletion (ADR-0002 never-invisible), datum-stable apex, contact-reuse/suspect fresh id, half-width floor, bounded range, prune-stale, math->true bearing. - Coastline: inside/outside/waterline~0.5/monotonic-across-shore/empty/point-in-hole-is-water. - ADR-0002 'never neither' end-to-end: tests/integration/test_static_obstacle_pipeline.cpp (phantom in footprint suppressed while real vessel in buffer still tracked + keep-clear alarm) plus the occupancy conservation-invariant unit test.  COULD NOT DETERMINE / minor: the CpaEvaluator 'dropped' Exited path (lines 66-79) emits an empty CpaPrediction (cpa_distance_m=0) and iterates a std::unordered_set to build the dropped list — the order of Exited events across multiple simultaneously-dropped tracks is hash-order dependent; whether any determinism/replay test compares the collision-event stream ordering was not confirmed (likely not part of the track-output determinism test, so low risk). The empty-payload contents of the deleted-track Exited event are not asserted anywhere.

### unit: adapters (4 findings)

- **[HIGH/CONFIRMED]** AIS SOG=1023 / COG=3600 velocity sentinels never fed to AisAdapter
  - `adapters/ais/AisAdapter.cpp:37` (, confidence ?)
  - description: The velocity path guards `has_speed = ... *r.sog_knots < 1023.0` (line 37-38) and `has_course = ... *r.cog_deg < 360.0` (line 39-40) are the ONLY thing stopping AIS 'not available' sentinel values from becoming PositionVelocity2D measurement content. I searched the whole test tree for `1023` and `3600` (grep across tests/, excluding the venv) — the only hits are unrelated occupancy-grid coordinates. tests/adapters/ais/test_ais_adapter.cpp exercises heading 511, nav_status 15, and lat91/lon181, but never feeds sog_knots=1023 or cog_deg=3600. The .hpp contract explicitly promises these are dropped.
  - verifier: The claimed gap is real. AisAdapter.cpp lines 37-40 hold the only guards (`*r.sog_knots < 1023.0`, `*r.cog_deg < 360.0`) stopping AIS velocity "not available" sentinels from entering PositionVelocity2D content, and AisAdapter.hpp lines 33-34 document these as a consumer contract ("1023 sentinel dropped", "3600 sentinel").  I searched for real coverage and found none: (1) `grep -rn '1023\|3600\|sog_knots\|cog_deg' tests/ --include=*.cpp` (venv excluded) — the only `3600` hit is tests/static/test_live_occupancy_model.cpp:809, an unrelated occupancy-grid coordinate; no `1023` hit exists anywhere in tests. Every sog_knots/cog_deg value in the whole test tree is valid (10.0, 0.2, 2.0, 90.0, 123.0), none at/above the guards. (2) I read tests/adapters/ais/test_ais_adapter.cpp in full: it exercises lat91/lon181, NaN, heading 511, nav_status 15, low-SOG fallback, and anchored/moored suppression — but never sog_knots≥1023 or cog_deg≥360. (3) I read tests/replay/test_ais_csv_replay_adapter.cpp: valid SOG/COG only.  No indirect coverage: the DropsSentinelAndNaNPositions test bails at line 23 (position plausibility) before the velocity block, so a regression of `< 1023.0` to `<=`/removal during a #20 refactor would leave every test green while a raw SOG sentinel becomes ~526 m/s emitted velocity, corrupting the target's velocity on the next update. The failure mode (silently wrong track) is exactly the exposed case for a consumer whose decoder passes the raw sentinel the contract promises to drop. Notably the test author covered the heading-511 and nav_status-15 sentinels but omitted the SOG/COG sentinel pair.

- **[HIGH/CONFIRMED]** ISO-8601 / BaseDateTime timestamp parsing in loadAisCsv is entirely untested
  - `adapters/replay/AisCsvReplayAdapter.cpp:93` (, confidence ?)
  - description: detectColumns (lines 60-61) advertises `basedatetime` / `timestamp` / `time` as time-column aliases — the MarineCadastre AIS export format — and parseTimeString has a full ISO-8601 branch (lines 93-115: sscanf date/time, fractional-seconds via strtod at the first '.', tm_mon = month-1 offset, timegm). I verified every AIS CSV fixture uses unix_time epoch floats (head of tests/fixtures/philos/out/ais_ferry_near/ais.csv = `unix_time,...` with `1667846694.361521`), and grep for `basedatetime`/ISO date patterns / `timegm` / `parseTimeString` across tests found nothing. The replay tests (test_philos_ospa, test_philos_farcross) call loadAisCsv but only on unix_time fixtures, so only the epoch branch runs.
  - verifier: The ISO-8601/BaseDateTime branch of parseTimeString (AisCsvReplayAdapter.cpp:93-116) and the basedatetime/timestamp/time column aliases (lines 60-61) are genuinely untested. I read the only dedicated test file, tests/replay/test_ais_csv_replay_adapter.cpp, in full: all 8 tests use CSVs with `unix_time,...` headers and epoch-float time values, exercising only the epoch branch (lines 86-91); none contains an ISO date, `basedatetime`, `timestamp`, or `time` column. parseTimeString is in an anonymous namespace and cannot be unit-tested directly — the only route is loadAisCsv with an ISO time column, which no test provides. Searches confirm no coverage: `grep -rni basedatetime` in tests/ (excluding readmes) = nothing; the only ISO-format date strings in non-fixture tests are test_csv_reader.cpp:13 / test_csv_writer.cpp:12 (`started_at_utc` benchmark run-metadata, unrelated to loadAisCsv). I enumerated every ais*.csv fixture header under tests/fixtures and data — all are unix_time, so the replay/scenario tests that call loadAisCsv (test_philos_ospa:155, test_philos_farcross:85/129, benchmark runners) also only hit the epoch branch; no integration path drives the ISO branch. The header (AisCsvReplayAdapter.hpp:19) documents MarineCadastre BaseDateTime as a supported surface, so this is not dead code. A regression in the tm_mon-1 month handling, fractional-second dot selection, or timegm timezone treatment (e.g. a `+02:00` offset silently taken as UTC) would produce wrong measurement timestamps and wrong OSPA/GOSPA for a standard AIS export, and no current test would catch it. The auditor did not miss existing coverage.

- **[MEDIUM/unverified]** Name-based CSV column detection never tested with a reordered/aliased header
  - `adapters/replay/AisCsvReplayAdapter.cpp:56` (, confidence ?)
  - description: loadAisCsv (detectColumns, lines 56-78) and loadOwnshipCsv (adapters/replay/OwnshipCsvReader.cpp lines 48-56) map columns by header name so column order is supposed to be irrelevant. Every unit test and every fixture uses the single canonical order `unix_time,mmsi,lat,lon,sog_mps,cog_deg,nav_status,name` (see all CSV literals in test_ais_csv_replay_adapter.cpp). No test presents lat/lon (or time) in a non-canonical position, and there is no dedicated OwnshipCsvReader unit test at all — it is only reached through fixed-order fixtures.

- **[LOW/unverified]** TTM speed/course unit variants K, S and relative-course 'R' unexercised
  - `adapters/arpa/ArpaAdapter.cpp:38` (, confidence ?)
  - description: distanceToMeters/speedToMps handle N/K/S codes (lines 29-43) and TTM course units 'R' add own heading (line 169). Grepping every RATTM literal in the tests shows units field is only ever N or K, speed>0 is fed only with N units, and course units are always 'T'. So speedToMps('K')/speedToMps('S') with nonzero speed, distanceToMeters('S'), and the relative-course->true 'R' branch are never executed.

Coverage notes (adapters): WELL COVERED (good news for synthesis): EdgeValidation is exhaustively tested both sides of every bound (lat/lon 90/180, NaN/Inf, range zero/negative/NaN/Inf, finite guard) in tests/adapters/util/test_edge_validation.cpp. AisAdapter: origin/covariance, sentinel+NaN position drop with valid interleaved fix, high/low-accuracy sigma, velocity emit above threshold (non-degenerate 4x4), low-SOG fallback, disable toggle, heading-511/nav-status-15 sentinel drop, anchored(1)/moored(5) velocity suppression vs underway(0) emit. AisCsvReplay: default-off byte-identical, opt-in velocity, low-SOG fallback, nav_status surface+15 drop, missing sog/cog columns tolerated, shared-helper no-drift proof against the NMEA path, anchored kinematic-identity ON-vs-OFF. ArpaAdapter: TLL position + out-of-range reject, TTM non-positive-range reject + position, published/unpublished/absent bias equivalence, config heading-std + per-pose heading floor/clamp (#16), GPS-std inflation, TLL-unaffected-by-GPS, birth-velocity seed + zero-speed no-seed, swap-diagnostic course-jump threshold. Static obstacles (adapters/static): extremely thorough — full/sparse attributes, non-Point/null skip, malformed JSON -> runtime_error, per-feature skip keeps following valid feature, out-of-range lat AND lon both sides, non-numeric coord skip, negative-radius clamp on all three radii, missing-file throw. Coastline (tests/land): Polygon + MultiPolygon + on-disk Boston fixture; [lon,lat] order verified via inside/outside prior. OwnShipNmeaAdapter: GGA position, HDT heading, malformed reject, HDOP*UERE, absent-HDOP-stays-zero, adaptive-UERE window publish/dominate/maneuver-fallback, RMC SOG/COG->ENU + zero-SOG sigma, RMC precedence/stale/estimator-fallback (multi-heading and bias-dispatch have their own files). RemoteTrackAdapter: covariance inflate/default, edge reject, per-track/per-station thinning, velocity opt-in on/off/default, circular-AIS MMSI guard. CameraBearingCsvReader: heading composition, wrap across north, no-pose drop, invalid-sigma reject, per-camera source ids. Replay loaders (AIS/plot/radar-truth/camera/ownship) all std::sort their output by time, so unsorted-input tolerance is structurally present and radar-truth has an explicit monotonic-after-sort assertion. NMEA parse: valid/bad-checksum/missing-delimiter covered. COULD NOT DETERMINE: did not execute the suite, so fixture-gated replay tests' actual run/skip status is unverified (fixtures ARE present in this main tree, so they should run). UNCERTAIN LOW: GeoJsonCoastline addPolygon parses interior rings (holes) but no adapter/coastline test confirms a point inside a hole is excluded from the land prior (a hole bug would suppress births over enclosed water); this straddles core CoastlineGeometry so I did not raise it as an adapter finding.

### unit: scenario-bench-sim (3 findings)

- **[MEDIUM/unverified]** No test verifies emitter-injected noise magnitude or its self-consistency with adapter-stamped R (NEES/NIS foundation)
  - `sim/AisEmitter.cpp:60` (, confidence ?)
  - description: Every emitter UNIT test (test_ais_emitter, test_arpa_emitter, test_eoir_emitter, test_own_ship_emitter) runs with pos_std_m / range_std_m / bearing_std_deg / gps_pos_std_m == 0 (noise OFF). The one covariance test (EoIrEmitter.BearingOnlyModeWidensRangeCovariance) checks the R *shape* on a noise-free measurement. The noisy paths are exercised only by the bus/GPS sweeps (test_bus_gps_sweep, test_bus_heading_sweep, test_bus_adaptive_uere), which mostly print tables and SUCCEED(); test_bus_regression asserts only a loose one-sided OSPA-ratio upper bound (baseline*~5, with 15% headroom). Nothing asserts that when you request std=X the empirical spread of emitted values around truth is X, nor that the emitter's injected std equals the covariance the adapter stamps on the resulting Measurement. The NEES/NIS consistency arm (alpha_hat, coverage_95) is validated in test_consistency only against hand-crafted IID-Gaussian InnovationEvents/BenchResult, never against the real emitter->adapter->tracker path; test_sweep only counts NIS/NEES rows.

- **[LOW/unverified]** No 'different seed -> different noise' test for the sensor emitters
  - `sim/ArpaEmitter.cpp:106` (, confidence ?)
  - description: Emitter determinism is covered (test_bus_determinism: same seed -> identical output), and SkewInjector has an explicit different-seeds-diverge test. But the AIS/ARPA/EO-IR/own-ship emitters have no test that two different seeds actually produce different noise realizations. A same-seed determinism test passes even if the seed is ignored entirely (e.g. rng_ constructed from a constant), so a dropped/hard-coded seed in an emitter would leave all seeds producing identical 'noise', silently collapsing the multi-seed sweep's variance estimate (AggStats std_ospa) to near-zero.

- **[LOW/unverified]** countIdSwitches silently ignores targets that appear after the first step
  - `core/scenario/Metrics.cpp:11` (, confidence ?)
  - description: countIdSwitches fixes k = steps.front().truth.size() and both allocates last(k) and loops `i < s.truth.size() && i < k`, slot-keying by index. Any truth slot beyond the step-0 cardinality is never scored, and if truth slot order varies per step the tally mixes physical targets. This is the headline id-switch metric consumed by the bus comparison tests (countIdSwitches(r.steps, cutoff) in BusComparisonHelpers and several test_bus_*). The tests exercise only fixed-cardinality-from-t0 scenarios; there is no test with a target appearing mid-scenario, and no test with per-step slot reordering. (The benchmark-side computeContinuity is correctly truth_id-keyed and IS tested for both cases, so the safe path exists — but the scenario path used by the bus suite is not guarded.)

Coverage notes (scenario-bench-sim): Scope covered: core/scenario/{Ospa,Gospa,TGospa,Metrics,TruthResample}, core/benchmark/{Metrics,Consistency,ExistenceLabel,Sweep,Config}, sim/{AisEmitter,ArpaEmitter,EoIrEmitter,OwnShipEmitter,NmeaEncode,SkewInjector} and matching tests in tests/{scenario,benchmark,sim}.  WELL COVERED (searched by symbol + scenario keyword + asserted-constant): - Metric kernels vs hand-computed values are excellent. Ospa: empty/single/two-pairs/clip + optimal-vs-greedy (test_ospa). Gospa scalar AND GospaComponents decomposition (localization/missed/false_/n_missed/n_false, alpha=1, optimal-assignment) hand-checked (test_gospa). TGospa: per-scan accumulation, switch penalty on assignment flip, all-missed/all-false cardinality (test_tgospa). benchmark id_switches/continuity/RMSE hand-checked incl. truth_id-keying vs slot reorder, time-varying cardinality, per-truth breakdown, and COG 90-deg wrap (test_metrics benchmark). countIdSwitches scenario-side: no-switch/one-switch/track-loss (test_metrics scenario). - TruthResample: linear position interp + finite-diff velocity, gap>max_gap breaks presence, single-fix nearest-tick, async targets share ticks, sorted+tick-contiguous determinism, empty/disabled (test_truth_resample). NOTE the flagged 'heading wrap' concern does NOT apply: TruthSample carries a velocity VECTOR (no heading angle is ever angularly interpolated), so there is no wrap hazard here. - Consistency NEES/NIS dof: m=1 and m=2 IID-Gaussian band tests, fitted-alpha at alpha in {0.5,1,4}, singular-S drop accounting, below-min band suppression, NEES-from-known-filter mean approx 2 (test_consistency). Wilson-Hilferty and exact chi2_1/chi2_2 one-sided quantiles are used and exercised. - ExistenceLabel timing + parsing: windowed/whole-clip/embedded-semicolons/notes/KEEP_MIXED/Unknown-default, activeAtUnix before/inside/after + whole-clip, and two committed fixtures (test_existence_label). - NmeaEncode round-trip: ddmm formatting incl. minute-carry, checksum wrap parses via real parseNmea (test_nmea_encode). Crucially the SEMANTIC encode->parse->value round-trip IS covered end-to-end elsewhere: ArpaEmitter RATTM -> ArpaAdapter -> Measurement value near truth (test_arpa_emitter ProducesMeasurementNearTruthInEnu), and GGA -> OwnShipProvider lat/lon within 1e-6 (test_own_ship_emitter). AIS/EO-IR emitters feed structured reports (no NMEA encode). - SkewInjector (clock skew): zero-profile identity, truth-timestamp preservation, same-seed determinism, different-seed reorder, single-sensor stable order, empty input, RemoteTrack indexability (test_skew_injector). - Sweep/BenchRunner config plumbing: the observable-output knobs ARE guarded end-to-end through runSweep by A/B tests that assert occ_* rows and cross-arm deltas (test_occupancy_ab, test_occupancy_detector_gates, test_los_guard_haxr_ab, test_veto_isolation_*), plus detectionModelFor per-sensor/source/clutter-map table plumbing and Config factory freshness/distinct-instance guards (test_sweep, test_config). Config OccupancyDetectorArmsDifferOnlyInCoverageFlag guards A/B validity of one knob.  COULD NOT FULLY DETERMINE: Config knobs that produce NO distinct output row and are not part of an A/B delta (e.g. use_sensor_activity_model / DeclaredSensorActivity wiring, and whether specific pmbm_config()/mht_config() field values provably change tracker behavior through runSweep rather than just being constructed) are only indirectly guarded by A/B configs that happen to differ. A silent failure to apply such a knob whose two arms otherwise coincide would not necessarily be caught, but I could not construct a concrete wrong-metric scenario without deeper PMBM-side tracing (out of this unit's source scope), so it is not reported as a finding.


## Batch T3-scenarios-assertions-registration

### unit: required-scenarios (5 findings)

- **[HIGH/REFUTED]** Head-on multi-target scenario is defined but never driven through a tracker with continuity/ID-stability/accuracy assertions
  - `tests/benchmark/test_sim_scenario_run.cpp:21` (, confidence ?)
  - description: CLAUDE.md lists head-on as a required scenario asserting track continuity, ID stability, and accuracy vs truth. The synthetic `head_on` scenario (adapters/benchmark/SimScenarioRun.cpp:131-148: two anti-parallel targets closing at ~50 m/s crossing at only 5 m lateral separation) exists ONLY as a definition — test_sim_scenario_run.cpp:21 asserts the label is present, and the sweep runs it but no test evaluates its tracked output (test_sweep.cpp uses only "tiny_line"; test_bus_regression covers crossing/overtaking/parallel/bearing-only, never head_on). The only other head-on test, tests/integration/test_full_stack_pipeline.cpp, is a SINGLE-target head-on and asserts manager.tracks().size()==1 — it cannot exercise the two-target ID-swap failure mode at all. tests/collision/test_cpa*.cpp reference "head-on" but only test CPA geometry, not the tracker. Searched: grep for head_on/HeadOn/head-on across tests/ (only CPA geometry + the label-count test); test_sweep/test_bus_regression/test_consistency assertion lines.
  - verifier: The claim's narrow literal statement — that the *named* `head_on` scenario object in adapters/benchmark/SimScenarioRun.cpp is never scored through a tracker — is technically true (test_sim_scenario_run.cpp:21 only counts its label; the sweep runs it but no test evaluates its tracked output). But the "gap that matters," per the claim's own why_it_matters, is the failure mode: two anti-parallel targets meeting head-on at close separation where the associator swaps the two track_ids, staying green because count stays 2 and OSPA stays low. That failure mode IS covered, so the claim is refuted.  REFUTING TEST: tests/scenario/test_crossing.cpp:16, `TEST(Stress, CrossingTargetsStayCountedAndIdsMostlyStable)`. It builds two targets via the SAME builder the head_on scenario uses — buildCrossingTargetsScenario — with anti-parallel velocities (25,0) and (-25,0) and lateral offset ±10 m (head_on uses the identical builder/velocities with ±5 m). Confirmed via Builders.cpp: the two targets move in straight lines directly toward and past each other, closing at 50 m/s and passing at 20 m separation at t=20s — this is head-on geometry, not a right-angle crossing (the "crossing" label is a geometric misnomer). It drives the scenario through the full EKF+GNN+CV2D tracker (est, GnnAssociator(50), Tracker gate 30) and asserts BOTH:   - EXPECT_EQ(mgr.size(), 2u)  — continuity/count   - EXPECT_LE(countIdSwitches(r.steps, 30.0), 2)  — ID stability  Why it catches the exact breakage the claim raises: countIdSwitches (core/scenario/Metrics.cpp:9) maps each truth target to its nearest track each step and increments when that truth's assigned track_id changes between steps. An associator ID swap at the close crossing (track A jumping to target 2, track B to target 1) flips both nearest-track_ids near CPA, driving switches above the ≤2 bound → red. This is precisely the ID-swap-with-stable-count-and-low-OSPA failure the claim says every test would miss; the OSPA-only bus-regression "Crossing" test would indeed miss it, but this test uses the swap-sensitive metric.  The claim's search grepped only for head_on/HeadOn/head-on and thus missed this test (named "Crossing" / "Stress"), and it explicitly dismissed the bus-regression crossing as "not head_on" without recognizing that both use anti-parallel head-on velocities. The verifier instructions warn test names often don't match source names — this is that case.  Secondary note: test_sim_multisensor_scenario_run.cpp:110 (HeadonFusionAccuracyGate) also drives a 2-vessel head-on through IMM+MHT asserting id_switches<6, but its fixture geometry passes at ~930 m minimum separation (verified from truth.csv) and is fixture-gated (skips without SIMMS_DIR), so it alone would NOT exercise the swap ambiguity — it is not the load-bearing refutation. test_crossing.cpp is, being unconditional (no fixtures) and at 20 m separation with the swap-sensitive metric.  The 5 m vs 10 m offset difference is not a distinct failure mode: at closing 50 m/s with 30 m tracker gate / 50 m association gate, both put the pair well within mutual gates at CPA, so both fully exercise the association ambiguity.

- **[MEDIUM/unverified]** Crossing ID-stability bound (<=2 switches) exactly admits a full two-target ID swap
  - `tests/scenario/test_crossing.cpp:34` (, confidence ?)
  - description: test_crossing asserts EXPECT_LE(countIdSwitches(r.steps,30.0),2) for the two crossing targets. countIdSwitches (core/scenario/Metrics.cpp) counts, per truth index, transitions of the nearest track_id from one nonzero value to a different nonzero value. A clean full ID swap of the two crossing targets produces exactly ONE counted switch per truth target = 2 total, which passes EXPECT_LE(...,2). So the assertion tolerates the precise failure (a crossing-point ID swap) it is meant to guard. Contrast test_overtaking.cpp:34 which uses ==0 (strict). mgr.size()==2 also survives a swap, so nothing in this test distinguishes clean tracking from a complete swap.

- **[MEDIUM/unverified]** PMBM determinism is asserted only on two aggregate scalars, not on per-step track IDs/positions
  - `tests/pmbm/test_pmbm_scenario.cpp:102` (, confidence ?)
  - description: PmbmScenario.ReplayDeterminism runs the same scenario twice and asserts only EXPECT_DOUBLE_EQ(mean_ospa) and EXPECT_EQ(final_track_count). The comment claims output must be "bit-identical" but the test never compares per-step track_ids or positions. The bench determinism test (tests/benchmark/test_bench_determinism.cpp:36) hashes all metric rows for ONLY configs.front(), which is imm_cv_ct_mht (Config.cpp:476-483) — so it covers the MHT path, not PMBM (imm_cv_ct_pmbm, Config.cpp:589). Thus the headline PMBM tracker's determinism (invariant #4) is guarded only by two scalars. Searched: all tests/pmbm/*.cpp and tests/benchmark/*determinism* for per-step/id comparison; none for PMBM.

- **[MEDIUM/unverified]** No end-to-end replay-determinism test through real adapters (NMEA/CSV in -> TrackOutput out, byte-identical twice)
  - `tests/sim/test_bus_determinism.cpp:62` (, confidence ?)
  - description: The CLAUDE.md requirement is "same log replayed twice -> identical output." test_bus_determinism asserts determinism of the sensor BUS measurement stream only (times/values), not tracker output. bench_determinism (test_bench_determinism.cpp) hashes metric ROWS from synthetic emitters for the MHT config — not raw TrackOutput and not through the real NMEA/CSV parse path. No test feeds a fixed NMEA or CSV log through adapter -> Tracker -> toTrackOutput twice and compares byte-identical TrackOutput (ids, lat/lon, covariance). Searched: tests/replay/*, tests/adapters/*, and *determinism* — the replay adapter tests (test_ais_csv_replay_adapter, test_autoferry_json_replay) assert parse correctness, not two-run output identity.

- **[LOW/unverified]** Non-cooperative (no-AIS, bearing-only) target has no tracked-output continuity/ID assertion
  - `tests/benchmark/test_sim_scenario_run.cpp:26` (, confidence ?)
  - description: The `non_cooperative` scenario (Bearing2D, no AIS) is only definition-tested (test_sim_scenario_run.cpp:26 label present; :158-161 model==Bearing2D). Its tracked output — that a target with no MMSI still gets tracked and kept with a stable id (ADR-0002 presence-over-classification) — is not asserted anywhere on this scenario. Bearing-only tracking ACCURACY is partially covered for a different single-target geometry by BusRegression.BearingOnlyMovingSensorMeanOspaWithinTolerance (test_bus_regression.cpp:269, OSPA-ratio tolerance only, no id-stability).

Coverage notes (required-scenarios): WELL COVERED (good news for synthesis): (1) crossing, overtaking, ais_dropout each have dedicated GNN-pipeline scenario tests (tests/scenario/) asserting mgr.size() and countIdSwitches; overtaking is strict (==0) and ais_dropout is strict (size==1, switches==0) — these two are strong. (2) Clock skew is well covered: tests/sim/test_skew_injector.cpp (injector unit incl. identity, determinism-same-seed, truth-timestamp preservation, RemoteTrack index bound) plus tests/scenario/test_reorder_buffer_e2e.cpp which asserts, under skew, determinism (stepsEqual: per-step id+position identical across two runs), monotonic drains, accuracy parity vs unskewed baseline (mean_ospa within tol), and drop counts matching an independently-recomputed ground-truth late count — genuinely strong. (3) MHT-path determinism is solid: test_bench_determinism hashes ALL non-wallclock metric rows across two full sweep runs of configs.front() (which is imm_cv_ct_mht, the canonical MHT — note the in-test comment \"ekf_cv_gnn\" is stale/misleading but harmless). (4) GNN single-hypothesis Tracker determinism is covered by reorder_buffer_e2e stepsEqual. (5) Head-on CPA GEOMETRY is well tested (tests/collision/test_cpa*.cpp). (6) Full-stack head-on single-target integration (test_full_stack_pipeline.cpp) strongly asserts lifecycle sequence, bias convergence, CPA Entered<Exited, and pull/push TrackOutput consistency. (7) Sim bus measurement-stream determinism (same seed identical, diff seed differs) is covered. countIdSwitches is a reasonable per-truth-target id-stability proxy (tracks nearest track_id per truth index over time). COULD NOT DETERMINE (read-only, no build): actual green/red status of any test; whether fixture-gated replay tests (philos/haxr/autoferry) actually run vs skip in this tree; whether the sim head_on/non_cooperative/speed_change scenarios feed any downstream A/B assertion I did not open (I checked test_sweep, test_consistency, test_bus_regression, test_bench_runner — none assert tracked output for head_on/non_cooperative). The five gaps reported are the required scenarios/paths whose tracked-output property is either unasserted (head_on, non_cooperative), asserted too loosely to catch the target failure (crossing swap tolerance), or asserted only at aggregate/measurement level rather than byte-identical output (PMBM determinism, full-adapter replay determinism).

### unit: assertion-quality (5 findings)

- **[HIGH/CONFIRMED]** Canonical crossing/overtaking tests assert continuity via a degenerate metric and never assert accuracy vs truth
  - `tests/scenario/test_crossing.cpp:34` (, confidence ?)
  - description: test_crossing.cpp (line 33-34) asserts only mgr.size()==2u and countIdSwitches(r.steps,30.0)<=2; test_overtaking.cpp:33-34 asserts mgr.size()==2u and countIdSwitches(...,30.0)==0. r.mean_ospa is computed by runScenario but discarded in both. countIdSwitches (core/scenario/Metrics.cpp) matches each truth to the nearest track ONLY within `cutoff` (30 m); when no track is within 30 m of a truth it sets best_id=0 and counts NO switch and does not update `last[i]`. So the switch count is 0 both when IDs are perfectly stable AND when both tracks have drifted permanently >30 m off truth. Combined with size==2 (which two ghost/clutter tracks also satisfy), an estimator/association/measurement-model regression that keeps two track objects alive but drives them far off truth passes green.
  - verifier: Verified all claim components against the tree. (1) countIdSwitches (core/scenario/Metrics.cpp:9-30) is degenerate exactly as described: when no track is within cutoff it sets best_id=0, counts no switch, and does not update last[i], so permanent drift >cutoff reads as 0 switches. (2) test_crossing.cpp:33-34 and test_overtaking.cpp:33-34 assert only mgr.size()==2u plus countIdSwitches; r.mean_ospa is discarded. (3) I grepped every use of buildCrossingTargetsScenario/buildOvertakingScenario across tests/ and read each: FilterComparison.Crossing asserts only count+id_switches (mean_ospa fprintf-only); MhtComparison.CrossingWithDropout is entirely print-only; BenchRunner JPDA test asserts only track presence (>50); test_reorder_buffer_e2e uses the builder for determinism not accuracy. The only absolute OSPA crossing gate is PmbmScenario.TwoTargetsCrossingTracked (EXPECT_LT mean_ospa<25.0), but that is the PMBM tracker on a different geometry — it does not cover the canonical EKF+GnnAssociator pipeline. BusRegression.Crossing/Overtaking gate mean_ospa only as a ratio (r < baseline*5.01) against a baseline recomputed from the same pipeline, which co-inflates and saturates at cutoff under an accuracy collapse, so it does not hard-gate absolute accuracy. Therefore an EKF/GNN estimator/association regression that keeps two tracks alive but drives them off truth on the crossing/overtaking geometry (classic GNN association degeneracy at the crossing point) ships uncaught. Minor caveat: a broad estimator collapse would also break the straight-line/parallel absolute OSPA gates (FilterComparison.SingleStraightLine <15, ParallelTargets <20), so only a geometry-specific crossing/overtaking regression truly escapes — but that is a real, decision-relevant failure mode, so the gap holds.

- **[MEDIUM/unverified]** Birth-existence-target out-of-range fallback test is fully vacuous when no Bernoulli is born
  - `tests/pmbm/test_pmbm_tracker_update.cpp:903` (, confidence ?)
  - description: TEST(PmbmTrackerUpdate, BirthExistenceTargetOutOfRangeFallsBackToLambdaBirth) sets birth_existence_target=1.5 (invalid) and expects the scalar lambda_birth fallback r=lambda_birth/(lambda_birth+lambda_C). Every assertion is gated by iterating tracker.density().mbm -> h.bernoullis and by the `if (any_bernoulli)` guard on the r_max EXPECT_NEAR. There is no ASSERT that any Bernoulli was actually born (no ASSERT_FALSE(mbm.empty()), no ASSERT_TRUE(any_bernoulli)). If a regression in the out-of-range guard causes the birth path to emit nothing, both the isfinite check and the r_expected check are skipped and the test passes green.

- **[MEDIUM/unverified]** Alt-birth 'no fresh birth in gated alt children' test skips all assertions if no gated-alt hypothesis exists
  - `tests/pmbm/test_pmbm_phase8.cpp:898` (, confidence ?)
  - description: In test_pmbm_phase8.cpp:897-905 the loop over tracker.density().mbm does `if (h.log_weight >= top_lw - 0.5) continue;` and only then runs EXPECT_NE(b.birth_time.seconds(), scan_t) on the inner Bernoullis. The outer ASSERT_FALSE(mbm.empty()) guarantees hypotheses exist, but NOT that any hypothesis is below the top-0.5 gate. If a regression collapses alt-hypothesis generation (all hypotheses within 0.5 log-weight of the top, or only the top hypothesis survives), every iteration hits `continue` and the fresh-birth assertion never executes -> vacuous pass.

- **[LOW/unverified]** Output-determinism hash truncates metric values to ostringstream default precision (~6 significant digits)
  - `tests/benchmark/test_bench_determinism.cpp:54` (, confidence ?)
  - description: hashRows() streams each MetricRow.value into a std::ostringstream with default formatting (os << r.value), i.e. ~6 significant digits, then hashes the string. This is the closest test to the CLAUDE.md 'replay of a log -> byte-identical output' invariant at the metrics level. A non-determinism that changes a metric below the 6th significant figure hashes identical and passes. It also covers only a single config (configs.front(), ekf_cv_gnn) and a single scenario (defaultSimScenarios().front()).

- **[LOW/unverified]** Several estimator/associator comparison TESTs contain zero assertions (print-only, can never fail)
  - `tests/scenario/test_filter_comparison.cpp:129` (, confidence ?)
  - description: test_filter_comparison.cpp ShortRangePass(129), VeryShortRangePass(158), BearingOnlyPass(211), ManeuveringTarget(240), Maneuvering3ModeIMM(280), ShortRangeMultiSeedSweep(334), BearingOnlyMovingSensor(391); plus test_jpda_comparison.cpp:42, test_mht_comparison.cpp:53, and all four test_multi_seed_sweep.cpp cases only fprintf metrics to stderr with no EXPECT/ASSERT. They are documented as comparison/reporting tools (metrics harness), so this is largely by design, but they run as gtest cases that pass unconditionally even if a filter produces NaN/diverged OSPA on these exact configurations.

Coverage notes (assertion-quality): WELL COVERED (good news for synthesis): (1) Sim-bus INPUT determinism is exact — tests/sim/test_bus_determinism.cpp uses EXPECT_DOUBLE_EQ on every measurement/truth value and asserts different seeds differ. (2) PMBM output determinism is exactly asserted (test_pmbm_tracker_update.cpp ReplayDeterministicallyReproducesState: EXPECT_EQ on Bernoulli ids + EXPECT_DOUBLE_EQ on existence — though not on kinematic mean/cov). (3) Unsorted-batch == sorted-batch equivalence is locked in for both MHT and PMBM with EXPECT_FALSE(empty) guards (tests/pipeline/test_batch_ordering.cpp). (4) ReorderBuffer ordering/late-drop is precisely asserted. (5) AIS-dropout continuity+ID stability, and parallel-targets accuracy (mean_ospa<20) are properly gated. (6) OSPA/GOSPA kernels are asserted against hand-computed constants at 1e-12 (test_ospa.cpp, test_gospa.cpp) — not self-comparisons. (7) Adapter edge validation (out-of-range lat/lon rejection, NaN) is asserted with EXPECT_FALSE. (8) The philos close_approach_labels KEEP_MIXED loop is protected by EXPECT_EQ(keep_mixed_seen,2) so it is NOT vacuous despite the filtered loop.\n\nNo DISABLED_ tests exist. All GTEST_SKIP paths are fixture-absence guards (fixtures ARE present in this main tree, so they run here); the only non-fixture skips are benchmark/test_los_guard_haxr_ab.cpp:111 and test_veto_isolation_sim_ab.cpp:57 ('scenario did not generate') and test_rbad_scenario_run.cpp (empty measurements) — could mask a scenario-generation regression as a silent skip, worth a glance but not a hard finding. The 4 EXPECT_NO_THROW uses are all paired with a subsequent value assertion (construct-then-check), so none are throw-only.\n\nCOULD NOT DETERMINE: I did not build/run the suite (read-only), so I did not confirm which fixture-gated replay tests actually execute vs skip in this tree, nor observe runtime values behind the loose-looking thresholds. The R-BAD EXPECT_GT(total_our_tracks,0) floor (test_rbad_scenario_run.cpp:122) is a deliberate non-gating consistency/reporting arm per project notes ('consistency not accuracy, no tuning'), so I did not report it as a defect.

### unit: registration-fixtures (4 findings)

- **[HIGH/CONFIRMED]** No mechanism catches a fixture-gated test that silently skipped when it should have run
  - `CMakeLists.txt:391` (, confidence ?)
  - description: Every scenario/replay/benchmark test uses the pattern `if (!fileExists(...)) GTEST_SKIP()`. There is NO test, script, CI step, or CTest fixture that asserts the expected set of tests actually RAN (not skipped): no `.github/`, no `ci/`, no shell scripts, no `set_tests_properties`/labels, and no tracked expected-skip manifest. gtest_discover_tests + `ctest` reports `100% tests passed` when a skip-guarded scenario or invariant test skips, because GTEST_SKIP counts as pass. The only 'guard' is a human diffing the skip count against prose in docs/algorithms/evaluation-log.md (e.g. 'Suite 1089 ran / 8 skipped'), which is not machine-checked.
  - verifier: I attempted to refute the claim by hunting for any machine-checked mechanism that asserts the expected set of fixture-gated tests actually RAN. I found none.  Where I searched and what I found: - Filesystem: no `.github/` and no `ci/` directory exist (ls of repo root; targeted find for *.yml/*.yaml/*ci* returned only unrelated paths and the vendored Stone-Soup `.circleci`/`.codecov.yml` under data/, which govern that third-party repo, not navtracker). - CTest properties: grep across all CMakeLists.txt/*.cmake for `set_tests_properties`, `LABELS`, `FIXTURES_REQUIRED`, `FIXTURES_SETUP`, `WILL_FAIL`, `SKIP_RETURN_CODE`, `REQUIRED_FILES` -> zero hits. Test registration is bare: `enable_testing()` + `gtest_discover_tests(navtracker_tests)` (CMakeLists.txt:22,392) with no extra args, plus one `add_test(CoreOnlyLinks ...)`. gtest_discover_tests with no SKIP handling means a GTEST_SKIP result is reported as a pass by ctest. - Skip pattern confirmed load-bearing: 48 GTEST_SKIP sites across tests/replay, tests/benchmark, tests/land, etc. Example tests/replay/test_haxr_ospa.cpp:68-72 does `if (!fileExists(...)) GTEST_SKIP()` on a hardcoded relative fixture path — a path typo/rename/missing fixture silently turns the OSPA assertion into a no-op that ctest counts as passed. - No expected-ran/expected-skip manifest: grep for `expected.skip|skip.manifest|must.run|expected.ran|tests ran|ran /|assert.*not.*skip|skip.*count` across tests/, tools/, docs/, CMakeLists.txt -> only unrelated hits (measurement-builder `expected` vars, source-level "SkipsUngated" behavior tests, docs prose). None is a meta-check on skip counts. - No tooling parses ctest/gtest output for skips: grep of tools/ and docs/superpowers/ for `SKIPPED|GTEST_SKIP|--gtest_list|ctest` returned only false positives; the one tools/ hit (autoferry_q_calibration.py) is about turn-rates skipped below MIN_SPEED_FOR_HEADING, not test skips. The only `.sh` files under the tree are fixture-extraction scripts (tests/fixtures/philos/batch_extract.sh, pohang_lidar/run_and_cleanup.sh) and vendored venv site-packages — none audit test execution. - No pytest conftest gating that would fail-not-skip.  The only "guard" is the human-facing convention captured in CLAUDE.md (feedback_verify_skip_list) and docs/algorithms/evaluation-log.md prose (diffing the skip count) — explicitly not machine-checked, which is exactly what the claim states. This is the documented failure that hid a red master for a day (2026-07-08). A load-bearing scenario/invariant test can be silently converted to a no-op by a fixture path typo, rename, unset env, or empty-returning loader, and nothing in the build/test system flags it; wrong-track or invariant regressions can ship behind a green ctest. The auditor did not miss existing coverage — no such coverage exists.

- **[HIGH/CONFIRMED]** sim-multisensor / imazu / rbad / haxr scenario gates resolve fixtures cwd-relative, so plain `ctest` skips them even though the fixtures are present in the tree
  - `adapters/benchmark/SimMultisensorScenarioRun.cpp:136` (, confidence ?)
  - description: Unlike the philos tests, which anchor fixtures absolutely via NAVTRACKER_SOURCE_DIR (tests/replay/PhilosLabelReplay.hpp:143 `srcDir()`), the sim-ms/imazu/rbad/haxr scenario loaders default to a CWD-RELATIVE path: `envOr("SIMMS_DIR", "tests/fixtures/sim_multisensor")` (SimMultisensorScenarioRun.cpp:136), `envOr("RBAD_DIR", "tests/fixtures/rbad")` (RbadScenarioRun.cpp:119), and test_haxr_ospa.cpp:39 `"tests/fixtures/haxr_cfar/out/..."`. `ctest`/gtest_discover_tests runs from the build directory, so those relative paths miss and generate() returns empty -> the tests GTEST_SKIP with 'unreachable (set SIMMS_DIR/RBAD_DIR)', even though tests/fixtures/{sim_multisensor,rbad,haxr_cfar} are present in the source tree. The source comments confirm this: 'Skips under ctest (cwd = build/, fixtures unreachable) unless SIMMS_DIR is set' (test_sim_multisensor_scenario_run.cpp:73).
  - verifier: Every link in the claim verified. The sim-ms/imazu/rbad/haxr loaders default to CWD-relative fixture paths (SimMultisensorScenarioRun.cpp:136 `envOr("SIMMS_DIR","tests/fixtures/sim_multisensor")`; RbadScenarioRun.cpp:119 `envOr("RBAD_DIR","tests/fixtures/rbad")`; test_haxr_ospa.cpp:39-41 `"tests/fixtures/haxr_cfar/..."` and `"data/dlr/..."`). By contrast the philos loader anchors absolutely via NAVTRACKER_SOURCE_DIR (PhilosLabelReplay.hpp:143 srcDir()), which the affected loaders never use. CMakeLists.txt:392 registers tests with `gtest_discover_tests(navtracker_tests)` — no WORKING_DIRECTORY and no ENVIRONMENT set, so tests execute from the build directory, where the CWD-relative paths miss; there is no file(COPY)/configure_file/symlink staging fixtures into the build tree. On a miss, generate() returns empty and the tests GTEST_SKIP — this includes HeadonFusionAccuracyGate (test_sim_multisensor_scenario_run.cpp:117-118, the first controlled fusion-accuracy gate), AnchoredAndCameraOnlyNeverInvisible (:149-150, the ADR-0002 never-suppressed-into-nothing invariant), and DeterministicReplay (:178-179) plus the Imazu/Rbad determinism variants. The source comments confirm the behavior ("Skips under ctest (cwd = build/, fixtures unreachable) unless SIMMS_DIR is set"). I searched tests/, .github/, CMakePresets.json, CTestConfig*, conanfile*, Makefile*, scripts/, tools/ for SIMMS_DIR/RBAD_DIR/HAXR_DIR/WORKING_DIRECTORY/ENVIRONMENT and for any reachability/no-skip guard test; found none. The env vars appear only in operator-run baseline docs, not in any CI or default build/test path. No test would catch a regression in these fusion/ADR-0002/determinism gates under a default ctest invocation.

- **[LOW/unverified]** No per-test TIMEOUT on any test; a hang has no watchdog
  - `CMakeLists.txt:392` (, confidence ?)
  - description: gtest_discover_tests is called with no DISCOVERY_TIMEOUT/PROPERTIES override and no `set_tests_properties(... TIMEOUT ...)` exists anywhere (grep for timeout/set_tests_properties across root and bench CMake returns nothing but a LINKER_LANGUAGE line). CTest's default per-test timeout is effectively unlimited. The scenario/replay/benchmark tests iterate over fixture-driven data and CSV/JSON loaders.

- **[LOW/unverified]** Foxglove tests (incl. recorder determinism) are dropped, not skipped, when NAVTRACKER_BUILD_FOXGLOVE=OFF
  - `CMakeLists.txt:381` (, confidence ?)
  - description: The five foxglove tests (test_geometry, test_foxglove_json, test_mcap_writer, test_recorder, test_recorder_determinism) are added to navtracker_tests only inside `if(NAVTRACKER_BUILD_FOXGLOVE)`. Building with -DNAVTRACKER_BUILD_FOXGLOVE=OFF removes them from the binary entirely (not a GTEST_SKIP) with no signal, and no other test covers the recorder determinism/serialization invariant in that config.

Coverage notes (registration-fixtures): WELL COVERED / good news: (1) Zero orphan tests — `comm -23` of `git ls-files tests/*.cpp` against test paths referenced in CMakeLists.txt is empty; all ~190 tracked test .cpp are compiled into navtracker_tests (plus tests/types/test_link_core_only.cpp as the separate CoreOnlyLinks executable). No silently-dead test files. (2) Parallel-safe (-j): no test writes into the source tree; temp writers use $TMPDIR / ::testing::TempDir(). TmpPath.hpp suffixes with PID+atomic counter; test_ais_csv_replay_adapter and test_autoferry_json_replay use per-test-unique filenames/labels, so no cross-test path collision under `ctest -j`. (3) The CLAUDE.md determinism invariant is covered by ALWAYS-ON synthetic tests that never fixture-gate: BenchDeterminism.RepeatedSweepProducesIdenticalRows, SimulatedSensorBusDeterminism.TwoRunsSameSeedIdenticalOutput, RecorderDeterminism.SameInputIdenticalPayloads. (4) philos/haxr LABEL replay tests anchor fixtures absolutely via NAVTRACKER_SOURCE_DIR (PhilosLabelReplay.hpp srcDir()), so with fixtures present they RUN under plain ctest — the correct pattern (contrast finding 2). (5) Config-struct drift is mechanically enforced by tests/docs/test_integration_guide_config_coverage.cpp. (6) bench/CMakeLists.txt registers only executables (no add_test), so nothing there is an unregistered ctest test.\n\nFIXTURE-SKIP INVENTORY (all GTEST_SKIP-gated cases): absolute-anchored (RUN when fixtures present in tree): tests/replay/test_philos_* (farcross, ospa, sunset_labels, close_approach_labels, close_approach_cpa, los_shadow_probe, occupancy_coverage_6c), test_camera_bearing_loader smoke, test_geojson_coastline BostonFixtureSmoke, test_radar_truth_loader, test_autoferry_json_replay LoadsRealScenario2 (data/autoferry present). CWD-RELATIVE/env-gated (SKIP under plain ctest from build/ despite fixtures present — see finding 2): test_haxr_ospa.KattwykHourEkfGnnBaseline, test_sim_multisensor_scenario_run.* (Generate…, AisMeasurementsCarryMmsiHint, HeadonFusionAccuracyGate, AnchoredAndCameraOnlyNeverInvisible=ADR-0002, DeterministicReplay), test_imazu22_scenario_run.* (4), test_rbad_scenario_run.* (3), test_clutter_burst_birth_confirm_probe.Race, test_philos_cluttermap_ab, test_replay_scenario_run.* (5), test_los_guard_haxr_ab, test_veto_isolation_haxr_ab, test_veto_isolation_sim_ab. Truly data-gated (data/ absent in this tree, e.g. data/haxr): the HAXR data-path variants.\n\nCOULD NOT DETERMINE: exact runtime skip count/names — the tree is read-only and I did not build or run ctest; the eval-log 'ran 1089 / 8 skipped' figures imply the team's verified runs export SIMMS_DIR (and skip only data/-gated HAXR/RBAD/Boston), which is consistent with finding 2's fragility but I could not reproduce it.


---

# Reconciliation disposition — 2026-07-15

The load-bearing D-gaps (each the coverage hole a confirmed bug lived in) were
backfilled as the fix-wave-5 Section-D deliverable (branch `fixwave-wave5`,
merged e4beac1) unless noted:

- Same-scan two-sensor fusion of one target, end-to-end — `SameScanTwoSensorFusionSingleTargetSinglePropagation` (W5.2 territory, `86b03ab`).
- AIS unavailable sentinels (SOG=1023 / COG=3600) fed to the adapter — W5.6.1 `64f3fe0` (impossible-band gate + regression pin).
- ISO-8601 / DMA `BaseDateTime` parsing in `loadAisCsv` — W5.6.2 `4c8adae`.
- ID-never-reused invariant (delete→create cycle) — W5.6.3 `df83762`.
- End-to-end replay determinism through the real adapters (NMEA/CSV in → TrackOutput out) — W5.6.4 `df83762`.
- Crossing/overtaking continuity metric — `test_overtaking.cpp` already asserts `countIdSwitches == 0` (strict, kept); `test_crossing.cpp` documents its drift-blind caveat in-code.
- **Residual (not wave-5):** sim-emitter noise-magnitude vs adapter-stamped covariance self-consistency (E-block) → wave-6 / eval-methodology; per-test ctest `TIMEOUT` watchdog (only the global 300→600 bump landed, W5.6.5 `97efbe9`) and remaining print-only/vacuous tests → improvement-backlog.
